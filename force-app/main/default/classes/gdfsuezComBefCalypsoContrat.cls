//Generated by FuseIT WSDL2Apex (http://www.fuseit.com/Solutions/SFDC-Explorer/Help-WSDL-Parser.aspx)

public class gdfsuezComBefCalypsoContrat {
  public class ContratAbstract {
    public String idContrat;
    private String[] idContrat_type_info = new String[]{'idContrat','http://gdfsuez.com/bef/calypso/contrat','','0','1','false'};
    private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};
    private String[] field_order_type_info = new String[]{'idContrat'};
    public ContratAbstract(){
    }
    public ContratAbstract(DOM.XmlNode responseNode){
      Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();
      DOM.XmlNode idContratNode = responseNode.getChildElement('idContrat', null);
      this.idContrat = (idContratNode == null) ? null : idContratNode.getText();
      nodesParsed.add(idContratNode);
      //System.debug(this.idContrat);
    }
    public void populateXmlNode(Dom.XmlNode outerNode){
      Dom.XmlNode idContratNode = outerNode.addChildElement('idContrat', 'http://gdfsuez.com/bef/calypso/contrat', null);
      idContratNode.addTextNode((this.idContrat == null) ? '' : this.idContrat);
    }
  }
  public class ContratRoot {
    public String idContrat;
    private String[] idContrat_type_info = new String[]{'idContrat','http://gdfsuez.com/bef/calypso/contrat','','0','1','false'};
    private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};
    private String[] field_order_type_info = new String[]{'idContrat'};
    public ContratRoot(){
    }
    public ContratRoot(DOM.XmlNode responseNode){
      Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();
      DOM.XmlNode idContratNode = responseNode.getChildElement('idContrat', 'http://gdfsuez.com/bef/calypso/contrat');
      this.idContrat = (idContratNode == null) ? null : idContratNode.getText();
      nodesParsed.add(idContratNode);
      //System.debug(this.idContrat);
    }
    public void populateXmlNode(Dom.XmlNode outerNode){
      Dom.XmlNode idContratNode = outerNode.addChildElement('idContrat', 'http://gdfsuez.com/bef/calypso/contrat', '');
      idContratNode.addTextNode((this.idContrat == null) ? '' : this.idContrat);
    }
  }
public static DateTime parseDateTime(String nodeText){

            String iso8601 = nodeText;

            JSONParser parser = JSON.createParser( '{"t":"' + iso8601 + '"}');

            parser.nextToken();

            parser.nextValue();

            DateTime dt = parser.getDateTimeValue();

            //System.debug('dt: ' + dt);

            return dt;

        }

    public class Contrat {

        public String idContrat;

        private String[] idContrat_type_info = new String[]{'idContrat','http://gdfsuez.com/bef/calypso/contrat','','0','1','false'};

        public gdfsuezComBefCalypsoContrat.CompteClientAbstract[] compteClient;

        public gdfsuezComBefCalypsoContrat.PackAbstract pack;

        public String duree;

        public gdfsuezComBefChpPivotContrat.listeValeursDt motifSouscrit;

        public gdfsuezComBefChpPivotContrat.listeValeursDt typeContrat;

       public String indicateurEnvoiAvenant;

        public DateTime dateDemenagementSouhaitee;

        public DateTime dateEmmenagementSouhaitee;

        public DateTime dateDebutContrat;

        public DateTime dateFinContrat;

        public DateTime dateCreationHistorique;

        public DateTime dureeHorsHistorique;

        public DateTime dateFinPrevisionnelle;

        public DateTime delaiResiliation;

        public DateTime entreeResiliation;

        public DateTime demandeResiliation;

        public DateTime dateEffetResiliation;

        public DateTime dateEffetContractuelleInitiale;

        public DateTime dateEffetContractuelle;

        public DateTime dateEcheance;

        public DateTime dateProposition;

        public DateTime debutAnciennePeriodeResilSansFrais;

        public DateTime debutNouvellePeriodeResilSansFrais;

        public DateTime finAnciennePeriodeResilSansFrais;

        public DateTime finNouvellePeriodeResilSansFrais;

        public DateTime dateCreationEntete;

        public DateTime dateCreationPoste;

        public Date dateNaissanceSouscripteur;

        public String codeCampagne;

        public gdfsuezComBefCalypsoContrat.Contrat_objetsLies_element[] objetsLies;

        public gdfsuezComBefCalypsoContrat.Contrat_accordClientConsommateur_element[] accordClientConsommateur;

        public gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_element[] composanteContrat;

        public gdfsuezComBefCalypsoContrat.RemiseOneShotDt[] remiseOneShot;

        public gdfsuezComBefCalypsoContrat.PersonneAbstract contractant;

        public gdfsuezComBefCalypsoContrat.PersonneAbstract conseillerOrigineCreation;

        public gdfsuezComBefCalypsoContrat.PersonneAbstract collaborateurResponsable;

        public String regionDonneurOrdre;

        public gdfsuezComBefChpPivotContrat.listeValeursDt traitementContrat;

        public gdfsuezComBefCalypsoContrat.dateSupplementaire[] dateSupplementaire;

        public gdfsuezComBefCalypsoContrat.Contrat_rolePersonne_element[] rolePersonne;

        public gdfsuezComBefCalypsoContrat.Contrat_TSE_element[] TSE;

        private String[] compteClient_type_info = new String[]{'compteClient','http://gdfsuez.com/bef/calypso/contrat','CompteClientAbstract','0','-1','false'};

        private String[] pack_type_info = new String[]{'pack','http://gdfsuez.com/bef/calypso/contrat','PackAbstract','0','1','false'};

        private String[] duree_type_info = new String[]{'duree','http://gdfsuez.com/bef/calypso/contrat','string13','0','1','false'};

        private String[] motifSouscrit_type_info = new String[]{'motifSouscrit','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] typeContrat_type_info = new String[]{'typeContrat','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] indicateurEnvoiAvenant_type_info = new String[]{'indicateurEnvoiAvenant','http://gdfsuez.com/bef/calypso/contrat','string1','0','1','false'};

        private String[] dateDemenagementSouhaitee_type_info = new String[]{'dateDemenagementSouhaitee','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateEmmenagementSouhaitee_type_info = new String[]{'dateEmmenagementSouhaitee','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateDebutContrat_type_info = new String[]{'dateDebutContrat','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateFinContrat_type_info = new String[]{'dateFinContrat','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateCreationHistorique_type_info = new String[]{'dateCreationHistorique','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dureeHorsHistorique_type_info = new String[]{'dureeHorsHistorique','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateFinPrevisionnelle_type_info = new String[]{'dateFinPrevisionnelle','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] delaiResiliation_type_info = new String[]{'delaiResiliation','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] entreeResiliation_type_info = new String[]{'entreeResiliation','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] demandeResiliation_type_info = new String[]{'demandeResiliation','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateEffetResiliation_type_info = new String[]{'dateEffetResiliation','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateEffetContractuelleInitiale_type_info = new String[]{'dateEffetContractuelleInitiale','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateEffetContractuelle_type_info = new String[]{'dateEffetContractuelle','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateEcheance_type_info = new String[]{'dateEcheance','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateProposition_type_info = new String[]{'dateProposition','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] debutAnciennePeriodeResilSansFrais_type_info = new String[]{'debutAnciennePeriodeResilSansFrais','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] debutNouvellePeriodeResilSansFrais_type_info = new String[]{'debutNouvellePeriodeResilSansFrais','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] finAnciennePeriodeResilSansFrais_type_info = new String[]{'finAnciennePeriodeResilSansFrais','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] finNouvellePeriodeResilSansFrais_type_info = new String[]{'finNouvellePeriodeResilSansFrais','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateCreationEntete_type_info = new String[]{'dateCreationEntete','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

       private String[] dateCreationPoste_type_info = new String[]{'dateCreationPoste','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateNaissanceSouscripteur_type_info = new String[]{'dateNaissanceSouscripteur','http://gdfsuez.com/bef/calypso/contrat','date','0','1','false'};

        private String[] codeCampagne_type_info = new String[]{'codeCampagne','http://gdfsuez.com/bef/calypso/contrat','string24','0','1','false'};

        private String[] objetsLies_type_info = new String[]{'objetsLies','http://gdfsuez.com/bef/calypso/contrat','','0','-1','false'};

        private String[] accordClientConsommateur_type_info = new String[]{'accordClientConsommateur','http://gdfsuez.com/bef/calypso/contrat','','0','-1','false'};

        private String[] composanteContrat_type_info = new String[]{'composanteContrat','http://gdfsuez.com/bef/calypso/contrat','','0','-1','false'};

        private String[] remiseOneShot_type_info = new String[]{'remiseOneShot','http://gdfsuez.com/bef/calypso/contrat','RemiseOneShotDt','0','-1','false'};

        private String[] contractant_type_info = new String[]{'contractant','http://gdfsuez.com/bef/calypso/contrat','PersonneAbstract','0','1','false'};

        private String[] conseillerOrigineCreation_type_info = new String[]{'conseillerOrigineCreation','http://gdfsuez.com/bef/calypso/contrat','PersonneAbstract','0','1','false'};

        private String[] collaborateurResponsable_type_info = new String[]{'collaborateurResponsable','http://gdfsuez.com/bef/calypso/contrat','PersonneAbstract','0','1','false'};

        private String[] regionDonneurOrdre_type_info = new String[]{'regionDonneurOrdre','http://gdfsuez.com/bef/calypso/contrat','string20','0','1','false'};

        private String[] traitementContrat_type_info = new String[]{'traitementContrat','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] dateSupplementaire_type_info = new String[]{'dateSupplementaire','http://gdfsuez.com/bef/calypso/contrat','dateSupplementaire','0','-1','false'};

        private String[] rolePersonne_type_info = new String[]{'rolePersonne','http://gdfsuez.com/bef/calypso/contrat','','0','-1','false'};

        private String[] TSE_type_info = new String[]{'TSE','http://gdfsuez.com/bef/calypso/contrat','','0','-1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'idContrat','compteClient','pack','duree','motifSouscrit','typeContrat','indicateurEnvoiAvenant','dateDemenagementSouhaitee','dateEmmenagementSouhaitee','dateDebutContrat','dateFinContrat','dateCreationHistorique','dureeHorsHistorique','dateFinPrevisionnelle','delaiResiliation','entreeResiliation','demandeResiliation','dateEffetResiliation','dateEffetContractuelleInitiale','dateEffetContractuelle','dateEcheance','dateProposition','debutAnciennePeriodeResilSansFrais','debutNouvellePeriodeResilSansFrais','finAnciennePeriodeResilSansFrais','finNouvellePeriodeResilSansFrais','dateCreationEntete','dateCreationPoste','dateNaissanceSouscripteur','codeCampagne','objetsLies','accordClientConsommateur','composanteContrat','remiseOneShot','contractant','conseillerOrigineCreation','collaborateurResponsable','regionDonneurOrdre','traitementContrat','dateSupplementaire','rolePersonne','TSE'};

        public Contrat(){

        }

        public Contrat(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode idContratNode = responseNode.getChildElement('idContrat', null);

            this.idContrat = (idContratNode == null) ? null : idContratNode.getText();

            nodesParsed.add(idContratNode);

            //System.debug(this.idContrat);

            Dom.XmlNode[] compteClientChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.CompteClientAbstract[] compteClientList = new List<gdfsuezComBefCalypsoContrat.CompteClientAbstract>();

            if(compteClientChildren == null || compteClientChildren.size() == 0){

                this.compteClient = null;

            } else{

                for(DOM.XmlNode childNode : compteClientChildren){

                    if(childNode.GetName() == 'compteClient'){

                        gdfsuezComBefCalypsoContrat.CompteClientAbstract Obj = new gdfsuezComBefCalypsoContrat.CompteClientAbstract(childNode);

                        compteClientList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.compteClient = compteClientList;

            }

            //System.debug(this.compteClient);

            DOM.XmlNode packNode = responseNode.getChildElement('pack', null);

            if(packNode == null){

                this.pack = null;

            } else{

                gdfsuezComBefCalypsoContrat.PackAbstract packObj = new gdfsuezComBefCalypsoContrat.PackAbstract(packNode);

                nodesParsed.add(packNode);

                this.pack = packObj;

            }

            //System.debug(this.pack);

            DOM.XmlNode dureeNode = responseNode.getChildElement('duree', null);

            this.duree = (dureeNode == null) ? null : dureeNode.getText();

            nodesParsed.add(dureeNode);

            //System.debug(this.duree);

            DOM.XmlNode motifSouscritNode = responseNode.getChildElement('motifSouscrit', null);

            if(motifSouscritNode == null){

                this.motifSouscrit = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt motifSouscritObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(motifSouscritNode);

                nodesParsed.add(motifSouscritNode);

                this.motifSouscrit = motifSouscritObj;

            }

            //System.debug(this.motifSouscrit);

            DOM.XmlNode typeContratNode = responseNode.getChildElement('typeContrat', null);

            if(typeContratNode == null){

                this.typeContrat = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt typeContratObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(typeContratNode);

                nodesParsed.add(typeContratNode);

                this.typeContrat = typeContratObj;

            }

            //System.debug(this.typeContrat);

            DOM.XmlNode indicateurEnvoiAvenantNode = responseNode.getChildElement('indicateurEnvoiAvenant', null);

            this.indicateurEnvoiAvenant = (indicateurEnvoiAvenantNode == null) ? null : indicateurEnvoiAvenantNode.getText();

            nodesParsed.add(indicateurEnvoiAvenantNode);

            //System.debug(this.indicateurEnvoiAvenant);

            DOM.XmlNode dateDemenagementSouhaiteeNode = responseNode.getChildElement('dateDemenagementSouhaitee', null);

            this.dateDemenagementSouhaitee = (dateDemenagementSouhaiteeNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateDemenagementSouhaiteeNode.getText());

            nodesParsed.add(dateDemenagementSouhaiteeNode);

            //System.debug(this.dateDemenagementSouhaitee);

            DOM.XmlNode dateEmmenagementSouhaiteeNode = responseNode.getChildElement('dateEmmenagementSouhaitee', null);

            this.dateEmmenagementSouhaitee = (dateEmmenagementSouhaiteeNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateEmmenagementSouhaiteeNode.getText());

            nodesParsed.add(dateEmmenagementSouhaiteeNode);

            //System.debug(this.dateEmmenagementSouhaitee);

            DOM.XmlNode dateDebutContratNode = responseNode.getChildElement('dateDebutContrat', null);

            this.dateDebutContrat = (dateDebutContratNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateDebutContratNode.getText());

            nodesParsed.add(dateDebutContratNode);

            //System.debug(this.dateDebutContrat);

            DOM.XmlNode dateFinContratNode = responseNode.getChildElement('dateFinContrat', null);

            this.dateFinContrat = (dateFinContratNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateFinContratNode.getText());

            nodesParsed.add(dateFinContratNode);

            //System.debug(this.dateFinContrat);

            DOM.XmlNode dateCreationHistoriqueNode = responseNode.getChildElement('dateCreationHistorique', null);

            this.dateCreationHistorique = (dateCreationHistoriqueNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateCreationHistoriqueNode.getText());

            nodesParsed.add(dateCreationHistoriqueNode);

            //System.debug(this.dateCreationHistorique);

            DOM.XmlNode dureeHorsHistoriqueNode = responseNode.getChildElement('dureeHorsHistorique', null);

            this.dureeHorsHistorique = (dureeHorsHistoriqueNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dureeHorsHistoriqueNode.getText());

            nodesParsed.add(dureeHorsHistoriqueNode);

            //System.debug(this.dureeHorsHistorique);

            DOM.XmlNode dateFinPrevisionnelleNode = responseNode.getChildElement('dateFinPrevisionnelle', null);

            this.dateFinPrevisionnelle = (dateFinPrevisionnelleNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateFinPrevisionnelleNode.getText());

            nodesParsed.add(dateFinPrevisionnelleNode);

            //System.debug(this.dateFinPrevisionnelle);

            DOM.XmlNode delaiResiliationNode = responseNode.getChildElement('delaiResiliation', null);

            this.delaiResiliation = (delaiResiliationNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(delaiResiliationNode.getText());

            nodesParsed.add(delaiResiliationNode);

            //System.debug(this.delaiResiliation);

            DOM.XmlNode entreeResiliationNode = responseNode.getChildElement('entreeResiliation', null);

            this.entreeResiliation = (entreeResiliationNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(entreeResiliationNode.getText());

            nodesParsed.add(entreeResiliationNode);

            //System.debug(this.entreeResiliation);

            DOM.XmlNode demandeResiliationNode = responseNode.getChildElement('demandeResiliation', null);

            this.demandeResiliation = (demandeResiliationNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(demandeResiliationNode.getText());

            nodesParsed.add(demandeResiliationNode);

            //System.debug(this.demandeResiliation);

            DOM.XmlNode dateEffetResiliationNode = responseNode.getChildElement('dateEffetResiliation', null);

            this.dateEffetResiliation = (dateEffetResiliationNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateEffetResiliationNode.getText());

            nodesParsed.add(dateEffetResiliationNode);

            //System.debug(this.dateEffetResiliation);

            DOM.XmlNode dateEffetContractuelleInitialeNode = responseNode.getChildElement('dateEffetContractuelleInitiale', null);

            this.dateEffetContractuelleInitiale = (dateEffetContractuelleInitialeNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateEffetContractuelleInitialeNode.getText());

            nodesParsed.add(dateEffetContractuelleInitialeNode);

            //System.debug(this.dateEffetContractuelleInitiale);

            DOM.XmlNode dateEffetContractuelleNode = responseNode.getChildElement('dateEffetContractuelle', null);

            this.dateEffetContractuelle = (dateEffetContractuelleNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateEffetContractuelleNode.getText());

            nodesParsed.add(dateEffetContractuelleNode);

            //System.debug(this.dateEffetContractuelle);

            DOM.XmlNode dateEcheanceNode = responseNode.getChildElement('dateEcheance', null);

            this.dateEcheance = (dateEcheanceNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateEcheanceNode.getText());

            nodesParsed.add(dateEcheanceNode);

            //System.debug(this.dateEcheance);

            DOM.XmlNode datePropositionNode = responseNode.getChildElement('dateProposition', null);

            this.dateProposition = (datePropositionNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(datePropositionNode.getText());

            nodesParsed.add(datePropositionNode);

            //System.debug(this.dateProposition);

            DOM.XmlNode debutAnciennePeriodeResilSansFraisNode = responseNode.getChildElement('debutAnciennePeriodeResilSansFrais', null);

            this.debutAnciennePeriodeResilSansFrais = (debutAnciennePeriodeResilSansFraisNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(debutAnciennePeriodeResilSansFraisNode.getText());

            nodesParsed.add(debutAnciennePeriodeResilSansFraisNode);

            //System.debug(this.debutAnciennePeriodeResilSansFrais);

            DOM.XmlNode debutNouvellePeriodeResilSansFraisNode = responseNode.getChildElement('debutNouvellePeriodeResilSansFrais', null);

            this.debutNouvellePeriodeResilSansFrais = (debutNouvellePeriodeResilSansFraisNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(debutNouvellePeriodeResilSansFraisNode.getText());

            nodesParsed.add(debutNouvellePeriodeResilSansFraisNode);

            //System.debug(this.debutNouvellePeriodeResilSansFrais);

            DOM.XmlNode finAnciennePeriodeResilSansFraisNode = responseNode.getChildElement('finAnciennePeriodeResilSansFrais', null);

            this.finAnciennePeriodeResilSansFrais = (finAnciennePeriodeResilSansFraisNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(finAnciennePeriodeResilSansFraisNode.getText());

            nodesParsed.add(finAnciennePeriodeResilSansFraisNode);

            //System.debug(this.finAnciennePeriodeResilSansFrais);

            DOM.XmlNode finNouvellePeriodeResilSansFraisNode = responseNode.getChildElement('finNouvellePeriodeResilSansFrais', null);

            this.finNouvellePeriodeResilSansFrais = (finNouvellePeriodeResilSansFraisNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(finNouvellePeriodeResilSansFraisNode.getText());

            nodesParsed.add(finNouvellePeriodeResilSansFraisNode);

            //System.debug(this.finNouvellePeriodeResilSansFrais);

            DOM.XmlNode dateCreationEnteteNode = responseNode.getChildElement('dateCreationEntete', null);

            this.dateCreationEntete = (dateCreationEnteteNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateCreationEnteteNode.getText());

            nodesParsed.add(dateCreationEnteteNode);

            //System.debug(this.dateCreationEntete);

            DOM.XmlNode dateCreationPosteNode = responseNode.getChildElement('dateCreationPoste', null);

            this.dateCreationPoste = (dateCreationPosteNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateCreationPosteNode.getText());

            nodesParsed.add(dateCreationPosteNode);

            //System.debug(this.dateCreationPoste);

            DOM.XmlNode dateNaissanceSouscripteurNode = responseNode.getChildElement('dateNaissanceSouscripteur', null);

            this.dateNaissanceSouscripteur = (dateNaissanceSouscripteurNode == null) ? null : Date.valueOf(dateNaissanceSouscripteurNode.getText());

            nodesParsed.add(dateNaissanceSouscripteurNode);

            //System.debug(this.dateNaissanceSouscripteur);

            DOM.XmlNode codeCampagneNode = responseNode.getChildElement('codeCampagne', null);

            this.codeCampagne = (codeCampagneNode == null) ? null : codeCampagneNode.getText();

            nodesParsed.add(codeCampagneNode);

            //System.debug(this.codeCampagne);

            Dom.XmlNode[] objetsLiesChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.Contrat_objetsLies_element[] objetsLiesList = new List<gdfsuezComBefCalypsoContrat.Contrat_objetsLies_element>();

            if(objetsLiesChildren == null || objetsLiesChildren.size() == 0){

                this.objetsLies = null;

            } else{

                for(DOM.XmlNode childNode : objetsLiesChildren){

                    if(childNode.GetName() == 'objetsLies'){

                        gdfsuezComBefCalypsoContrat.Contrat_objetsLies_element Obj = new gdfsuezComBefCalypsoContrat.Contrat_objetsLies_element(childNode);

                        objetsLiesList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.objetsLies = objetsLiesList;

            }

            //System.debug(this.objetsLies);

            Dom.XmlNode[] accordClientConsommateurChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.Contrat_accordClientConsommateur_element[] accordClientConsommateurList = new List<gdfsuezComBefCalypsoContrat.Contrat_accordClientConsommateur_element>();

            if(accordClientConsommateurChildren == null || accordClientConsommateurChildren.size() == 0){

                this.accordClientConsommateur = null;

            } else{

                for(DOM.XmlNode childNode : accordClientConsommateurChildren){

                    if(childNode.GetName() == 'accordClientConsommateur'){

                        gdfsuezComBefCalypsoContrat.Contrat_accordClientConsommateur_element Obj = new gdfsuezComBefCalypsoContrat.Contrat_accordClientConsommateur_element(childNode);

                        accordClientConsommateurList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.accordClientConsommateur = accordClientConsommateurList;

            }

            //System.debug(this.accordClientConsommateur);

            Dom.XmlNode[] composanteContratChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_element[] composanteContratList = new List<gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_element>();

            if(composanteContratChildren == null || composanteContratChildren.size() == 0){

                this.composanteContrat = null;

            } else{

                for(DOM.XmlNode childNode : composanteContratChildren){

                    if(childNode.GetName() == 'composanteContrat'){

                        gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_element Obj = new gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_element(childNode);

                        composanteContratList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.composanteContrat = composanteContratList;

            }

            //System.debug(this.composanteContrat);

            Dom.XmlNode[] remiseOneShotChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.RemiseOneShotDt[] remiseOneShotList = new List<gdfsuezComBefCalypsoContrat.RemiseOneShotDt>();

            if(remiseOneShotChildren == null || remiseOneShotChildren.size() == 0){

                this.remiseOneShot = null;

            } else{

                for(DOM.XmlNode childNode : remiseOneShotChildren){

                    if(childNode.GetName() == 'remiseOneShot'){

                        gdfsuezComBefCalypsoContrat.RemiseOneShotDt Obj = new gdfsuezComBefCalypsoContrat.RemiseOneShotDt(childNode);

                        remiseOneShotList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.remiseOneShot = remiseOneShotList;

            }

            //System.debug(this.remiseOneShot);

            DOM.XmlNode contractantNode = responseNode.getChildElement('contractant', null);

            if(contractantNode == null){

                this.contractant = null;

            } else{

                gdfsuezComBefCalypsoContrat.PersonneAbstract contractantObj = new gdfsuezComBefCalypsoContrat.PersonneAbstract(contractantNode);

                nodesParsed.add(contractantNode);

                this.contractant = contractantObj;

            }

            //System.debug(this.contractant);

            DOM.XmlNode conseillerOrigineCreationNode = responseNode.getChildElement('conseillerOrigineCreation', null);

            if(conseillerOrigineCreationNode == null){

                this.conseillerOrigineCreation = null;

            } else{

                gdfsuezComBefCalypsoContrat.PersonneAbstract conseillerOrigineCreationObj = new gdfsuezComBefCalypsoContrat.PersonneAbstract(conseillerOrigineCreationNode);

                nodesParsed.add(conseillerOrigineCreationNode);

                this.conseillerOrigineCreation = conseillerOrigineCreationObj;

            }

            //System.debug(this.conseillerOrigineCreation);

            DOM.XmlNode collaborateurResponsableNode = responseNode.getChildElement('collaborateurResponsable', null);

            if(collaborateurResponsableNode == null){

                this.collaborateurResponsable = null;

            } else{

                gdfsuezComBefCalypsoContrat.PersonneAbstract collaborateurResponsableObj = new gdfsuezComBefCalypsoContrat.PersonneAbstract(collaborateurResponsableNode);

                nodesParsed.add(collaborateurResponsableNode);

                this.collaborateurResponsable = collaborateurResponsableObj;

            }

            //System.debug(this.collaborateurResponsable);

            DOM.XmlNode regionDonneurOrdreNode = responseNode.getChildElement('regionDonneurOrdre', null);

            this.regionDonneurOrdre = (regionDonneurOrdreNode == null) ? null : regionDonneurOrdreNode.getText();

            nodesParsed.add(regionDonneurOrdreNode);

            //System.debug(this.regionDonneurOrdre);

            DOM.XmlNode traitementContratNode = responseNode.getChildElement('traitementContrat', null);

            if(traitementContratNode == null){

                this.traitementContrat = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt traitementContratObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(traitementContratNode);

                nodesParsed.add(traitementContratNode);

                this.traitementContrat = traitementContratObj;

            }

            //System.debug(this.traitementContrat);

            Dom.XmlNode[] dateSupplementaireChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.dateSupplementaire[] dateSupplementaireList = new List<gdfsuezComBefCalypsoContrat.dateSupplementaire>();

            if(dateSupplementaireChildren == null || dateSupplementaireChildren.size() == 0){

                this.dateSupplementaire = null;

            } else{

                for(DOM.XmlNode childNode : dateSupplementaireChildren){

                    if(childNode.GetName() == 'dateSupplementaire'){

                        gdfsuezComBefCalypsoContrat.dateSupplementaire Obj = new gdfsuezComBefCalypsoContrat.dateSupplementaire(childNode);

                        dateSupplementaireList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.dateSupplementaire = dateSupplementaireList;

            }

            //System.debug(this.dateSupplementaire);

            Dom.XmlNode[] rolePersonneChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.Contrat_rolePersonne_element[] rolePersonneList = new List<gdfsuezComBefCalypsoContrat.Contrat_rolePersonne_element>();

            if(rolePersonneChildren == null || rolePersonneChildren.size() == 0){

                this.rolePersonne = null;

            } else{

                for(DOM.XmlNode childNode : rolePersonneChildren){

                    if(childNode.GetName() == 'rolePersonne'){

                        gdfsuezComBefCalypsoContrat.Contrat_rolePersonne_element Obj = new gdfsuezComBefCalypsoContrat.Contrat_rolePersonne_element(childNode);

                        rolePersonneList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.rolePersonne = rolePersonneList;

            }

            //System.debug(this.rolePersonne);

            Dom.XmlNode[] TSEChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.Contrat_TSE_element[] TSEList = new List<gdfsuezComBefCalypsoContrat.Contrat_TSE_element>();

            if(TSEChildren == null || TSEChildren.size() == 0){

                this.TSE = null;

            } else{

                for(DOM.XmlNode childNode : TSEChildren){

                    if(childNode.GetName() == 'TSE'){

                        gdfsuezComBefCalypsoContrat.Contrat_TSE_element Obj = new gdfsuezComBefCalypsoContrat.Contrat_TSE_element(childNode);

                        TSEList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.TSE = TSEList;

            }

            //System.debug(this.TSE);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode idContratNode = outerNode.addChildElement('idContrat', 'http://gdfsuez.com/bef/calypso/contrat', '');

            idContratNode.addTextNode((this.idContrat == null) ? '' : this.idContrat);



            gdfsuezComBefCalypsoContrat.CompteClientAbstract[] compteClientObj = this.compteClient;

            if(compteClientObj != null){

                for(gdfsuezComBefCalypsoContrat.CompteClientAbstract element: compteClientObj){

                    Dom.XmlNode compteClientNode = outerNode.addChildElement('compteClient', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(compteClientNode);

                }

            }



            gdfsuezComBefCalypsoContrat.PackAbstract packObj = this.pack;

            Dom.XmlNode packNode = outerNode.addChildElement('pack', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(packObj != null){

                packObj.populateXmlNode(packNode);

            }

            Dom.XmlNode dureeNode = outerNode.addChildElement('duree', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dureeNode.addTextNode((this.duree == null) ? '' : this.duree);



            gdfsuezComBefChpPivotContrat.listeValeursDt motifSouscritObj = this.motifSouscrit;

            Dom.XmlNode motifSouscritNode = outerNode.addChildElement('motifSouscrit', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(motifSouscritObj != null){

                motifSouscritObj.populateXmlNode(motifSouscritNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt typeContratObj = this.typeContrat;

            Dom.XmlNode typeContratNode = outerNode.addChildElement('typeContrat', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(typeContratObj != null){

                typeContratObj.populateXmlNode(typeContratNode);

            }

            Dom.XmlNode indicateurEnvoiAvenantNode = outerNode.addChildElement('indicateurEnvoiAvenant', 'http://gdfsuez.com/bef/calypso/contrat', '');

            indicateurEnvoiAvenantNode.addTextNode((this.indicateurEnvoiAvenant == null) ? '' : this.indicateurEnvoiAvenant);

            Dom.XmlNode dateDemenagementSouhaiteeNode = outerNode.addChildElement('dateDemenagementSouhaitee', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateDemenagementSouhaiteeNode.addTextNode((this.dateDemenagementSouhaitee == null) ? '' : String.valueOf(this.dateDemenagementSouhaitee));

            Dom.XmlNode dateEmmenagementSouhaiteeNode = outerNode.addChildElement('dateEmmenagementSouhaitee', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateEmmenagementSouhaiteeNode.addTextNode((this.dateEmmenagementSouhaitee == null) ? '' : String.valueOf(this.dateEmmenagementSouhaitee));

            Dom.XmlNode dateDebutContratNode = outerNode.addChildElement('dateDebutContrat', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateDebutContratNode.addTextNode((this.dateDebutContrat == null) ? '' : String.valueOf(this.dateDebutContrat));

            Dom.XmlNode dateFinContratNode = outerNode.addChildElement('dateFinContrat', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateFinContratNode.addTextNode((this.dateFinContrat == null) ? '' : String.valueOf(this.dateFinContrat));

            Dom.XmlNode dateCreationHistoriqueNode = outerNode.addChildElement('dateCreationHistorique', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateCreationHistoriqueNode.addTextNode((this.dateCreationHistorique == null) ? '' : String.valueOf(this.dateCreationHistorique));

            Dom.XmlNode dureeHorsHistoriqueNode = outerNode.addChildElement('dureeHorsHistorique', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dureeHorsHistoriqueNode.addTextNode((this.dureeHorsHistorique == null) ? '' : String.valueOf(this.dureeHorsHistorique));

            Dom.XmlNode dateFinPrevisionnelleNode = outerNode.addChildElement('dateFinPrevisionnelle', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateFinPrevisionnelleNode.addTextNode((this.dateFinPrevisionnelle == null) ? '' : String.valueOf(this.dateFinPrevisionnelle));

            Dom.XmlNode delaiResiliationNode = outerNode.addChildElement('delaiResiliation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            delaiResiliationNode.addTextNode((this.delaiResiliation == null) ? '' : String.valueOf(this.delaiResiliation));

            Dom.XmlNode entreeResiliationNode = outerNode.addChildElement('entreeResiliation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            entreeResiliationNode.addTextNode((this.entreeResiliation == null) ? '' : String.valueOf(this.entreeResiliation));

            Dom.XmlNode demandeResiliationNode = outerNode.addChildElement('demandeResiliation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            demandeResiliationNode.addTextNode((this.demandeResiliation == null) ? '' : String.valueOf(this.demandeResiliation));

            Dom.XmlNode dateEffetResiliationNode = outerNode.addChildElement('dateEffetResiliation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateEffetResiliationNode.addTextNode((this.dateEffetResiliation == null) ? '' : String.valueOf(this.dateEffetResiliation));

            Dom.XmlNode dateEffetContractuelleInitialeNode = outerNode.addChildElement('dateEffetContractuelleInitiale', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateEffetContractuelleInitialeNode.addTextNode((this.dateEffetContractuelleInitiale == null) ? '' : String.valueOf(this.dateEffetContractuelleInitiale));

            Dom.XmlNode dateEffetContractuelleNode = outerNode.addChildElement('dateEffetContractuelle', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateEffetContractuelleNode.addTextNode((this.dateEffetContractuelle == null) ? '' : String.valueOf(this.dateEffetContractuelle));

            Dom.XmlNode dateEcheanceNode = outerNode.addChildElement('dateEcheance', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateEcheanceNode.addTextNode((this.dateEcheance == null) ? '' : String.valueOf(this.dateEcheance));

            Dom.XmlNode datePropositionNode = outerNode.addChildElement('dateProposition', 'http://gdfsuez.com/bef/calypso/contrat', '');

            datePropositionNode.addTextNode((this.dateProposition == null) ? '' : String.valueOf(this.dateProposition));

            Dom.XmlNode debutAnciennePeriodeResilSansFraisNode = outerNode.addChildElement('debutAnciennePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat', '');

            debutAnciennePeriodeResilSansFraisNode.addTextNode((this.debutAnciennePeriodeResilSansFrais == null) ? '' : String.valueOf(this.debutAnciennePeriodeResilSansFrais));

            Dom.XmlNode debutNouvellePeriodeResilSansFraisNode = outerNode.addChildElement('debutNouvellePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat', '');

            debutNouvellePeriodeResilSansFraisNode.addTextNode((this.debutNouvellePeriodeResilSansFrais == null) ? '' : String.valueOf(this.debutNouvellePeriodeResilSansFrais));

            Dom.XmlNode finAnciennePeriodeResilSansFraisNode = outerNode.addChildElement('finAnciennePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat', '');

            finAnciennePeriodeResilSansFraisNode.addTextNode((this.finAnciennePeriodeResilSansFrais == null) ? '' : String.valueOf(this.finAnciennePeriodeResilSansFrais));

            Dom.XmlNode finNouvellePeriodeResilSansFraisNode = outerNode.addChildElement('finNouvellePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat', '');

            finNouvellePeriodeResilSansFraisNode.addTextNode((this.finNouvellePeriodeResilSansFrais == null) ? '' : String.valueOf(this.finNouvellePeriodeResilSansFrais));

            Dom.XmlNode dateCreationEnteteNode = outerNode.addChildElement('dateCreationEntete', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateCreationEnteteNode.addTextNode((this.dateCreationEntete == null) ? '' : String.valueOf(this.dateCreationEntete));

            Dom.XmlNode dateCreationPosteNode = outerNode.addChildElement('dateCreationPoste', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateCreationPosteNode.addTextNode((this.dateCreationPoste == null) ? '' : String.valueOf(this.dateCreationPoste));

            Dom.XmlNode dateNaissanceSouscripteurNode = outerNode.addChildElement('dateNaissanceSouscripteur', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateNaissanceSouscripteurNode.addTextNode((this.dateNaissanceSouscripteur == null) ? '' : String.valueOf(this.dateNaissanceSouscripteur));

            Dom.XmlNode codeCampagneNode = outerNode.addChildElement('codeCampagne', 'http://gdfsuez.com/bef/calypso/contrat', '');

            codeCampagneNode.addTextNode((this.codeCampagne == null) ? '' : this.codeCampagne);



            gdfsuezComBefCalypsoContrat.Contrat_objetsLies_element[] objetsLiesObj = this.objetsLies;

            if(objetsLiesObj != null){

                for(gdfsuezComBefCalypsoContrat.Contrat_objetsLies_element element: objetsLiesObj){

                    Dom.XmlNode objetsLiesNode = outerNode.addChildElement('objetsLies', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(objetsLiesNode);

                }

            }



            gdfsuezComBefCalypsoContrat.Contrat_accordClientConsommateur_element[] accordClientConsommateurObj = this.accordClientConsommateur;

            if(accordClientConsommateurObj != null){

                for(gdfsuezComBefCalypsoContrat.Contrat_accordClientConsommateur_element element: accordClientConsommateurObj){

                    Dom.XmlNode accordClientConsommateurNode = outerNode.addChildElement('accordClientConsommateur', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(accordClientConsommateurNode);

                }

            }



            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_element[] composanteContratObj = this.composanteContrat;

            if(composanteContratObj != null){

                for(gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_element element: composanteContratObj){

                    Dom.XmlNode composanteContratNode = outerNode.addChildElement('composanteContrat', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(composanteContratNode);

                }

            }



            gdfsuezComBefCalypsoContrat.RemiseOneShotDt[] remiseOneShotObj = this.remiseOneShot;

            if(remiseOneShotObj != null){

                for(gdfsuezComBefCalypsoContrat.RemiseOneShotDt element: remiseOneShotObj){

                    Dom.XmlNode remiseOneShotNode = outerNode.addChildElement('remiseOneShot', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(remiseOneShotNode);

                }

            }



            gdfsuezComBefCalypsoContrat.PersonneAbstract contractantObj = this.contractant;

            Dom.XmlNode contractantNode = outerNode.addChildElement('contractant', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(contractantObj != null){

                contractantObj.populateXmlNode(contractantNode);

            }



            gdfsuezComBefCalypsoContrat.PersonneAbstract conseillerOrigineCreationObj = this.conseillerOrigineCreation;

            Dom.XmlNode conseillerOrigineCreationNode = outerNode.addChildElement('conseillerOrigineCreation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(conseillerOrigineCreationObj != null){

                conseillerOrigineCreationObj.populateXmlNode(conseillerOrigineCreationNode);

            }



            gdfsuezComBefCalypsoContrat.PersonneAbstract collaborateurResponsableObj = this.collaborateurResponsable;

            Dom.XmlNode collaborateurResponsableNode = outerNode.addChildElement('collaborateurResponsable', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(collaborateurResponsableObj != null){

                collaborateurResponsableObj.populateXmlNode(collaborateurResponsableNode);

            }

            Dom.XmlNode regionDonneurOrdreNode = outerNode.addChildElement('regionDonneurOrdre', 'http://gdfsuez.com/bef/calypso/contrat', '');

            regionDonneurOrdreNode.addTextNode((this.regionDonneurOrdre == null) ? '' : this.regionDonneurOrdre);



            gdfsuezComBefChpPivotContrat.listeValeursDt traitementContratObj = this.traitementContrat;

            Dom.XmlNode traitementContratNode = outerNode.addChildElement('traitementContrat', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(traitementContratObj != null){

                traitementContratObj.populateXmlNode(traitementContratNode);

            }



            gdfsuezComBefCalypsoContrat.dateSupplementaire[] dateSupplementaireObj = this.dateSupplementaire;

            if(dateSupplementaireObj != null){

                for(gdfsuezComBefCalypsoContrat.dateSupplementaire element: dateSupplementaireObj){

                    Dom.XmlNode dateSupplementaireNode = outerNode.addChildElement('dateSupplementaire', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(dateSupplementaireNode);

                }

            }



            gdfsuezComBefCalypsoContrat.Contrat_rolePersonne_element[] rolePersonneObj = this.rolePersonne;

            if(rolePersonneObj != null){

                for(gdfsuezComBefCalypsoContrat.Contrat_rolePersonne_element element: rolePersonneObj){

                    Dom.XmlNode rolePersonneNode = outerNode.addChildElement('rolePersonne', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(rolePersonneNode);

                }

            }



            gdfsuezComBefCalypsoContrat.Contrat_TSE_element[] TSEObj = this.TSE;

            if(TSEObj != null){

                for(gdfsuezComBefCalypsoContrat.Contrat_TSE_element element: TSEObj){

                    Dom.XmlNode TSENode = outerNode.addChildElement('TSE', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(TSENode);

                }

            }

        }

    }

    public class Contrat_accordClientConsommateur_element {

        public gdfsuezComBefChpPivotContrat.listeValeursDt etatAccordClient;

        public gdfsuezComBefChpPivotContrat.listeValeursDt energie;

        public gdfsuezComBefChpPivotContrat.listeValeursDt canalCollecte;

        public DateTime dateDerniereMAJ;

        private String[] etatAccordClient_type_info = new String[]{'etatAccordClient','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','1','1','false'};

        private String[] energie_type_info = new String[]{'energie','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] canalCollecte_type_info = new String[]{'canalCollecte','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] dateDerniereMAJ_type_info = new String[]{'dateDerniereMAJ','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'etatAccordClient','energie','canalCollecte','dateDerniereMAJ'};

        public Contrat_accordClientConsommateur_element(){

        }

        public Contrat_accordClientConsommateur_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode etatAccordClientNode = responseNode.getChildElement('etatAccordClient', null);

            if(etatAccordClientNode == null){

                this.etatAccordClient = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt etatAccordClientObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(etatAccordClientNode);

                nodesParsed.add(etatAccordClientNode);

                this.etatAccordClient = etatAccordClientObj;

            }

            //System.debug(this.etatAccordClient);

            DOM.XmlNode energieNode = responseNode.getChildElement('energie', null);

            if(energieNode == null){

                this.energie = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt energieObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(energieNode);

                nodesParsed.add(energieNode);

                this.energie = energieObj;

            }

            //System.debug(this.energie);

            DOM.XmlNode canalCollecteNode = responseNode.getChildElement('canalCollecte', null);

            if(canalCollecteNode == null){

                this.canalCollecte = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt canalCollecteObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(canalCollecteNode);

                nodesParsed.add(canalCollecteNode);

                this.canalCollecte = canalCollecteObj;

            }

            //System.debug(this.canalCollecte);

            DOM.XmlNode dateDerniereMAJNode = responseNode.getChildElement('dateDerniereMAJ', null);

            this.dateDerniereMAJ = (dateDerniereMAJNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateDerniereMAJNode.getText());

            nodesParsed.add(dateDerniereMAJNode);

            //System.debug(this.dateDerniereMAJ);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){



            gdfsuezComBefChpPivotContrat.listeValeursDt etatAccordClientObj = this.etatAccordClient;

            Dom.XmlNode etatAccordClientNode = outerNode.addChildElement('etatAccordClient', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(etatAccordClientObj != null){

                etatAccordClientObj.populateXmlNode(etatAccordClientNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt energieObj = this.energie;

            Dom.XmlNode energieNode = outerNode.addChildElement('energie', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(energieObj != null){

                energieObj.populateXmlNode(energieNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt canalCollecteObj = this.canalCollecte;

            Dom.XmlNode canalCollecteNode = outerNode.addChildElement('canalCollecte', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(canalCollecteObj != null){

                canalCollecteObj.populateXmlNode(canalCollecteNode);

            }

            Dom.XmlNode dateDerniereMAJNode = outerNode.addChildElement('dateDerniereMAJ', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateDerniereMAJNode.addTextNode((this.dateDerniereMAJ == null) ? '' : String.valueOf(this.dateDerniereMAJ));

        }

    }

    public class Contrat_composanteContrat_blocage_element {

        public DateTime dateDebutBlocage;

        public DateTime dateFinBlocage;

        public gdfsuezComBefChpPivotContrat.listeValeursDt motifBlocage;

        private String[] dateDebutBlocage_type_info = new String[]{'dateDebutBlocage','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateFinBlocage_type_info = new String[]{'dateFinBlocage','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] motifBlocage_type_info = new String[]{'motifBlocage','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

       private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'dateDebutBlocage','dateFinBlocage','motifBlocage'};

        public Contrat_composanteContrat_blocage_element(){

        }

        public Contrat_composanteContrat_blocage_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode dateDebutBlocageNode = responseNode.getChildElement('dateDebutBlocage', null);

            this.dateDebutBlocage = (dateDebutBlocageNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateDebutBlocageNode.getText());

           nodesParsed.add(dateDebutBlocageNode);

            //System.debug(this.dateDebutBlocage);

            DOM.XmlNode dateFinBlocageNode = responseNode.getChildElement('dateFinBlocage', null);

            this.dateFinBlocage = (dateFinBlocageNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateFinBlocageNode.getText());

            nodesParsed.add(dateFinBlocageNode);

            //System.debug(this.dateFinBlocage);

            DOM.XmlNode motifBlocageNode = responseNode.getChildElement('motifBlocage', null);

            if(motifBlocageNode == null){

                this.motifBlocage = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt motifBlocageObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(motifBlocageNode);

                nodesParsed.add(motifBlocageNode);

                this.motifBlocage = motifBlocageObj;

            }

            //System.debug(this.motifBlocage);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode dateDebutBlocageNode = outerNode.addChildElement('dateDebutBlocage', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateDebutBlocageNode.addTextNode((this.dateDebutBlocage == null) ? '' : String.valueOf(this.dateDebutBlocage));

            Dom.XmlNode dateFinBlocageNode = outerNode.addChildElement('dateFinBlocage', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateFinBlocageNode.addTextNode((this.dateFinBlocage == null) ? '' : String.valueOf(this.dateFinBlocage));



            gdfsuezComBefChpPivotContrat.listeValeursDt motifBlocageObj = this.motifBlocage;

            Dom.XmlNode motifBlocageNode = outerNode.addChildElement('motifBlocage', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(motifBlocageObj != null){

                motifBlocageObj.populateXmlNode(motifBlocageNode);

            }

        }

    }

    public class Contrat_composanteContrat_caracteristiquePME_element {

        public String valeur;

        public String code;

        private String[] valeur_type_info = new String[]{'valeur','http://gdfsuez.com/bef/calypso/contrat','string','0','1','false'};

        private String[] code_type_info = new String[]{'code','http://gdfsuez.com/bef/calypso/contrat','string','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'valeur','code'};

        public Contrat_composanteContrat_caracteristiquePME_element(){

        }

        public Contrat_composanteContrat_caracteristiquePME_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode valeurNode = responseNode.getChildElement('valeur', null);

            this.valeur = (valeurNode == null) ? null : valeurNode.getText();

            nodesParsed.add(valeurNode);

            //System.debug(this.valeur);

            DOM.XmlNode codeNode = responseNode.getChildElement('code', null);

            this.code = (codeNode == null) ? null : codeNode.getText();

            nodesParsed.add(codeNode);

            //System.debug(this.code);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode valeurNode = outerNode.addChildElement('valeur', 'http://gdfsuez.com/bef/calypso/contrat', '');

            valeurNode.addTextNode((this.valeur == null) ? '' : this.valeur);

            Dom.XmlNode codeNode = outerNode.addChildElement('code', 'http://gdfsuez.com/bef/calypso/contrat', '');

            codeNode.addTextNode((this.code == null) ? '' : this.code);

        }

    }

    public class Contrat_composanteContrat_element {

        public String numContratISU;

        public String uniteReleve;

        public gdfsuezComBefCalypsoContrat.PDLAbstract PDL;

        public gdfsuezComBefChpPivotContrat.listeValeursDt secteurActivite;

        public gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_blocage_element[] blocage;

        public gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_element[] historiqueComposante;

        public gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_caracteristiquePME_element[] caracteristiquePME;

        private String[] numContratISU_type_info = new String[]{'numContratISU','http://gdfsuez.com/bef/calypso/contrat','string10','0','1','false'};

        private String[] uniteReleve_type_info = new String[]{'uniteReleve','http://gdfsuez.com/bef/calypso/contrat','string8','0','1','false'};

        private String[] PDL_type_info = new String[]{'PDL','http://gdfsuez.com/bef/calypso/contrat','PDLAbstract','0','1','false'};

        private String[] secteurActivite_type_info = new String[]{'secteurActivite','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] blocage_type_info = new String[]{'blocage','http://gdfsuez.com/bef/calypso/contrat','','0','-1','false'};

        private String[] historiqueComposante_type_info = new String[]{'historiqueComposante','http://gdfsuez.com/bef/calypso/contrat','','0','-1','false'};

        private String[] caracteristiquePME_type_info = new String[]{'caracteristiquePME','http://gdfsuez.com/bef/calypso/contrat','','0','-1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'numContratISU','uniteReleve','PDL','secteurActivite','blocage','historiqueComposante','caracteristiquePME'};

        public Contrat_composanteContrat_element(){

        }

        public Contrat_composanteContrat_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode numContratISUNode = responseNode.getChildElement('numContratISU', null);

            this.numContratISU = (numContratISUNode == null) ? null : numContratISUNode.getText();

            nodesParsed.add(numContratISUNode);

            //System.debug(this.numContratISU);

            DOM.XmlNode uniteReleveNode = responseNode.getChildElement('uniteReleve', null);

            this.uniteReleve = (uniteReleveNode == null) ? null : uniteReleveNode.getText();

            nodesParsed.add(uniteReleveNode);

            //System.debug(this.uniteReleve);

            DOM.XmlNode PDLNode = responseNode.getChildElement('PDL', null);

            if(PDLNode == null){

                this.PDL = null;

            } else{

                gdfsuezComBefCalypsoContrat.PDLAbstract PDLObj = new gdfsuezComBefCalypsoContrat.PDLAbstract(PDLNode);

                nodesParsed.add(PDLNode);

                this.PDL = PDLObj;

            }

            //System.debug(this.PDL);

            DOM.XmlNode secteurActiviteNode = responseNode.getChildElement('secteurActivite', null);

            if(secteurActiviteNode == null){

                this.secteurActivite = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt secteurActiviteObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(secteurActiviteNode);

                nodesParsed.add(secteurActiviteNode);

                this.secteurActivite = secteurActiviteObj;

            }

            //System.debug(this.secteurActivite);

            Dom.XmlNode[] blocageChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_blocage_element[] blocageList = new List<gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_blocage_element>();

            if(blocageChildren == null || blocageChildren.size() == 0){

                this.blocage = null;

            } else{

                for(DOM.XmlNode childNode : blocageChildren){

                    if(childNode.GetName() == 'blocage'){

                        gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_blocage_element Obj = new gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_blocage_element(childNode);

                        blocageList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.blocage = blocageList;

            }

            //System.debug(this.blocage);

            Dom.XmlNode[] historiqueComposanteChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_element[] historiqueComposanteList = new List<gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_element>();

            if(historiqueComposanteChildren == null || historiqueComposanteChildren.size() == 0){

                this.historiqueComposante = null;

            } else{

                for(DOM.XmlNode childNode : historiqueComposanteChildren){

                    if(childNode.GetName() == 'historiqueComposante'){

                        gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_element Obj = new gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_element(childNode);

                        historiqueComposanteList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.historiqueComposante = historiqueComposanteList;

            }

            //System.debug(this.historiqueComposante);

            Dom.XmlNode[] caracteristiquePMEChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_caracteristiquePME_element[] caracteristiquePMEList = new List<gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_caracteristiquePME_element>();

            if(caracteristiquePMEChildren == null || caracteristiquePMEChildren.size() == 0){

                this.caracteristiquePME = null;

            } else{

                for(DOM.XmlNode childNode : caracteristiquePMEChildren){

                    if(childNode.GetName() == 'caracteristiquePME'){

                        gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_caracteristiquePME_element Obj = new gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_caracteristiquePME_element(childNode);

                        caracteristiquePMEList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.caracteristiquePME = caracteristiquePMEList;

            }

            //System.debug(this.caracteristiquePME);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode numContratISUNode = outerNode.addChildElement('numContratISU', 'http://gdfsuez.com/bef/calypso/contrat', '');

            numContratISUNode.addTextNode((this.numContratISU == null) ? '' : this.numContratISU);

            Dom.XmlNode uniteReleveNode = outerNode.addChildElement('uniteReleve', 'http://gdfsuez.com/bef/calypso/contrat', '');

            uniteReleveNode.addTextNode((this.uniteReleve == null) ? '' : this.uniteReleve);



            gdfsuezComBefCalypsoContrat.PDLAbstract PDLObj = this.PDL;

            Dom.XmlNode PDLNode = outerNode.addChildElement('PDL', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(PDLObj != null){

                PDLObj.populateXmlNode(PDLNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt secteurActiviteObj = this.secteurActivite;

            Dom.XmlNode secteurActiviteNode = outerNode.addChildElement('secteurActivite', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(secteurActiviteObj != null){

                secteurActiviteObj.populateXmlNode(secteurActiviteNode);

            }



            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_blocage_element[] blocageObj = this.blocage;

            if(blocageObj != null){

                for(gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_blocage_element element: blocageObj){

                    Dom.XmlNode blocageNode = outerNode.addChildElement('blocage', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(blocageNode);

                }

            }



            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_element[] historiqueComposanteObj = this.historiqueComposante;

            if(historiqueComposanteObj != null){

                for(gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_element element: historiqueComposanteObj){

                    Dom.XmlNode historiqueComposanteNode = outerNode.addChildElement('historiqueComposante', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(historiqueComposanteNode);

                }

            }



            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_caracteristiquePME_element[] caracteristiquePMEObj = this.caracteristiquePME;

            if(caracteristiquePMEObj != null){

                for(gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_caracteristiquePME_element element: caracteristiquePMEObj){

                    Dom.XmlNode caracteristiquePMENode = outerNode.addChildElement('caracteristiquePME', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(caracteristiquePMENode);

                }

            }

        }

    }

    public class Contrat_composanteContrat_historiqueComposante_cadransCalendrierFournisseur_element {

        public gdfsuezComBefChpPivotContrat.listeValeursDt[] cadran;

        private String[] cadran_type_info = new String[]{'cadran','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','-1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'cadran'};

        public Contrat_composanteContrat_historiqueComposante_cadransCalendrierFournisseur_element(){

        }

        public Contrat_composanteContrat_historiqueComposante_cadransCalendrierFournisseur_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            Dom.XmlNode[] cadranChildren = responseNode.getChildElements();

            gdfsuezComBefChpPivotContrat.listeValeursDt[] cadranList = new List<gdfsuezComBefChpPivotContrat.listeValeursDt>();

            if(cadranChildren == null || cadranChildren.size() == 0){

                this.cadran = null;

            } else{

                for(DOM.XmlNode childNode : cadranChildren){

                    if(childNode.GetName() == 'cadran'){

                        gdfsuezComBefChpPivotContrat.listeValeursDt Obj = new gdfsuezComBefChpPivotContrat.listeValeursDt(childNode);

                        cadranList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.cadran = cadranList;

            }

            //System.debug(this.cadran);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){



            gdfsuezComBefChpPivotContrat.listeValeursDt[] cadranObj = this.cadran;

            if(cadranObj != null){

                for(gdfsuezComBefChpPivotContrat.listeValeursDt element: cadranObj){

                    Dom.XmlNode cadranNode = outerNode.addChildElement('cadran', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(cadranNode);

                }

            }

        }

    }

    public class Contrat_composanteContrat_historiqueComposante_clePrix_element {

        public gdfsuezComBefChpPivotContrat.listeValeursDt typeClePrix;

        public String valeurClePrix;

        private String[] typeClePrix_type_info = new String[]{'typeClePrix','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] valeurClePrix_type_info = new String[]{'valeurClePrix','http://gdfsuez.com/bef/calypso/contrat','string40','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'typeClePrix','valeurClePrix'};

        public Contrat_composanteContrat_historiqueComposante_clePrix_element(){

        }

        public Contrat_composanteContrat_historiqueComposante_clePrix_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode typeClePrixNode = responseNode.getChildElement('typeClePrix', null);

            if(typeClePrixNode == null){

                this.typeClePrix = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt typeClePrixObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(typeClePrixNode);

                nodesParsed.add(typeClePrixNode);

                this.typeClePrix = typeClePrixObj;

            }

            //System.debug(this.typeClePrix);

            DOM.XmlNode valeurClePrixNode = responseNode.getChildElement('valeurClePrix', null);

            this.valeurClePrix = (valeurClePrixNode == null) ? null : valeurClePrixNode.getText();

            nodesParsed.add(valeurClePrixNode);

            //System.debug(this.valeurClePrix);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){



            gdfsuezComBefChpPivotContrat.listeValeursDt typeClePrixObj = this.typeClePrix;

            Dom.XmlNode typeClePrixNode = outerNode.addChildElement('typeClePrix', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(typeClePrixObj != null){

                typeClePrixObj.populateXmlNode(typeClePrixNode);

            }

            Dom.XmlNode valeurClePrixNode = outerNode.addChildElement('valeurClePrix', 'http://gdfsuez.com/bef/calypso/contrat', '');

            valeurClePrixNode.addTextNode((this.valeurClePrix == null) ? '' : this.valeurClePrix);

        }

    }

    public class Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_consommationAnnuelleEstimeeCadran_element {

        public String consommationAnnuelleEstimee;

        public gdfsuezComBefChpPivotContrat.listeValeursDt cadran;

        private String[] consommationAnnuelleEstimee_type_info = new String[]{'consommationAnnuelleEstimee','http://gdfsuez.com/bef/calypso/contrat','string10','1','1','false'};

        private String[] cadran_type_info = new String[]{'cadran','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','1','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'consommationAnnuelleEstimee','cadran'};

        public Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_consommationAnnuelleEstimeeCadran_element(){

        }

        public Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_consommationAnnuelleEstimeeCadran_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode consommationAnnuelleEstimeeNode = responseNode.getChildElement('consommationAnnuelleEstimee', null);

            this.consommationAnnuelleEstimee = (consommationAnnuelleEstimeeNode == null) ? null : consommationAnnuelleEstimeeNode.getText();

            nodesParsed.add(consommationAnnuelleEstimeeNode);

            //System.debug(this.consommationAnnuelleEstimee);

            DOM.XmlNode cadranNode = responseNode.getChildElement('cadran', null);

            if(cadranNode == null){

                this.cadran = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt cadranObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(cadranNode);

                nodesParsed.add(cadranNode);

                this.cadran = cadranObj;

            }

            //System.debug(this.cadran);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode consommationAnnuelleEstimeeNode = outerNode.addChildElement('consommationAnnuelleEstimee', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(this.consommationAnnuelleEstimee != null){

                consommationAnnuelleEstimeeNode.addTextNode(this.consommationAnnuelleEstimee);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt cadranObj = this.cadran;

            Dom.XmlNode cadranNode = outerNode.addChildElement('cadran', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(cadranObj != null){

                cadranObj.populateXmlNode(cadranNode);

            }

        }

    }

    public class Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_element {

        public gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_consommationAnnuelleEstimeeCadran_element consommationAnnuelleEstimeeCadran;

        private String[] consommationAnnuelleEstimeeCadran_type_info = new String[]{'consommationAnnuelleEstimeeCadran','http://gdfsuez.com/bef/calypso/contrat','','1','10','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'consommationAnnuelleEstimeeCadran'};

        public Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_element(){

        }

        public Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode consommationAnnuelleEstimeeCadranNode = responseNode.getChildElement('consommationAnnuelleEstimeeCadran', null);

            if(consommationAnnuelleEstimeeCadranNode == null){

                this.consommationAnnuelleEstimeeCadran = null;

            } else{

                gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_consommationAnnuelleEstimeeCadran_element consommationAnnuelleEstimeeCadranObj = new gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_consommationAnnuelleEstimeeCadran_element(consommationAnnuelleEstimeeCadranNode);

                nodesParsed.add(consommationAnnuelleEstimeeCadranNode);

                this.consommationAnnuelleEstimeeCadran = consommationAnnuelleEstimeeCadranObj;

            }

            //System.debug(this.consommationAnnuelleEstimeeCadran);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){



            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_consommationAnnuelleEstimeeCadran_element consommationAnnuelleEstimeeCadranObj = this.consommationAnnuelleEstimeeCadran;

            Dom.XmlNode consommationAnnuelleEstimeeCadranNode = outerNode.addChildElement('consommationAnnuelleEstimeeCadran', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(consommationAnnuelleEstimeeCadranObj != null){

                consommationAnnuelleEstimeeCadranObj.populateXmlNode(consommationAnnuelleEstimeeCadranNode);

            }

        }

    }

    public class Contrat_composanteContrat_historiqueComposante_element {

        public String identifiantHistorique;

        public gdfsuezComBefChpPivotContrat.listeValeursDt typeComposante;

        public gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_clePrix_element[] clePrix;

        public gdfsuezComBefChpPivotContrat.listeValeursDt calendrierFournisseur;

        public gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_cadransCalendrierFournisseur_element cadransCalendrierFournisseur;

        public gdfsuezComBefChpPivotContrat.listeValeursDt tarifAcheminement;

        public gdfsuezComBefChpPivotContrat.listeValeursDt puissance;

        public gdfsuezComBefChpPivotContrat.listeValeursDt versionUtilisation;

        public gdfsuezComBefChpPivotContrat.listeValeursDt profilConsommation;

        public gdfsuezComBefChpPivotContrat.listeValeursDt statutComposante;

        public gdfsuezComBefChpPivotContrat.listeValeursDt typeComptage;

        public gdfsuezComBefChpPivotContrat.listeValeursDt plageConsommation;

        public String consommationAnnuelleReference;

        public String nombreCertificatsVerts;

        public gdfsuezComBefChpPivotContrat.listeValeursDt motifResiliation;

        public DateTime dateCreationHistorique;

        public DateTime dateDemenagementSouhaitee;

        public DateTime dateEmmenagementSouhaitee;

        public DateTime dateDebutContrat;

        public DateTime dateFinContrat;

        public DateTime dureeHorsHistorique;

        public DateTime dateFinPrevisionnelle;

        public DateTime delaiResiliation;

        public DateTime entreeResiliation;

        public DateTime demandeResiliation;

        public DateTime dateEffetResiliation;

        public DateTime dateEffetContractuelleInitiale;

        public DateTime dateEffetContractuelle;

        public DateTime dateEcheance;

        public DateTime dateProposition;

        public DateTime debutAnciennePeriodeResilSansFrais;

        public DateTime debutNouvellePeriodeResilSansFrais;

        public DateTime finAnciennePeriodeResilSansFrais;

        public DateTime finNouvellePeriodeResilSansFrais;

        public DateTime dateCreationEntete;

        public DateTime dateCreationPoste;

        public String consommationAnnuelleEstimeeGaz;

        public String consommationAnnuelleEstimeeElec;

        public gdfsuezComBefChpPivotContrat.listeValeursDt FTA;

        public gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_element consommationAnnuelleEstimeeCadrans;

        public gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_remiseRecurrente_element[] remiseRecurrente;

        public gdfsuezComBefCalypsoContrat.RemiseOneShotDt[] remiseOneShot;

        public gdfsuezComBefCalypsoContrat.dateSupplementaire[] dateComposanteSupplementaire;

        public gdfsuezComBefCalypsoContrat.OffreAbstract offre;

        public String CARD;

        private String[] identifiantHistorique_type_info = new String[]{'identifiantHistorique','http://gdfsuez.com/bef/calypso/contrat','string10','0','1','false'};

        private String[] typeComposante_type_info = new String[]{'typeComposante','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] clePrix_type_info = new String[]{'clePrix','http://gdfsuez.com/bef/calypso/contrat','','0','-1','false'};

        private String[] calendrierFournisseur_type_info = new String[]{'calendrierFournisseur','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] cadransCalendrierFournisseur_type_info = new String[]{'cadransCalendrierFournisseur','http://gdfsuez.com/bef/calypso/contrat','','0','1','false'};

        private String[] tarifAcheminement_type_info = new String[]{'tarifAcheminement','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] puissance_type_info = new String[]{'puissance','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] versionUtilisation_type_info = new String[]{'versionUtilisation','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] profilConsommation_type_info = new String[]{'profilConsommation','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] statutComposante_type_info = new String[]{'statutComposante','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] typeComptage_type_info = new String[]{'typeComptage','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] plageConsommation_type_info = new String[]{'plageConsommation','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] consommationAnnuelleReference_type_info = new String[]{'consommationAnnuelleReference','http://gdfsuez.com/bef/calypso/contrat','string40','0','1','false'};

        private String[] nombreCertificatsVerts_type_info = new String[]{'nombreCertificatsVerts','http://gdfsuez.com/bef/calypso/contrat','string40','0','1','false'};

        private String[] motifResiliation_type_info = new String[]{'motifResiliation','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] dateCreationHistorique_type_info = new String[]{'dateCreationHistorique','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateDemenagementSouhaitee_type_info = new String[]{'dateDemenagementSouhaitee','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateEmmenagementSouhaitee_type_info = new String[]{'dateEmmenagementSouhaitee','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateDebutContrat_type_info = new String[]{'dateDebutContrat','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateFinContrat_type_info = new String[]{'dateFinContrat','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dureeHorsHistorique_type_info = new String[]{'dureeHorsHistorique','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateFinPrevisionnelle_type_info = new String[]{'dateFinPrevisionnelle','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] delaiResiliation_type_info = new String[]{'delaiResiliation','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] entreeResiliation_type_info = new String[]{'entreeResiliation','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] demandeResiliation_type_info = new String[]{'demandeResiliation','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateEffetResiliation_type_info = new String[]{'dateEffetResiliation','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateEffetContractuelleInitiale_type_info = new String[]{'dateEffetContractuelleInitiale','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateEffetContractuelle_type_info = new String[]{'dateEffetContractuelle','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateEcheance_type_info = new String[]{'dateEcheance','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateProposition_type_info = new String[]{'dateProposition','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] debutAnciennePeriodeResilSansFrais_type_info = new String[]{'debutAnciennePeriodeResilSansFrais','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] debutNouvellePeriodeResilSansFrais_type_info = new String[]{'debutNouvellePeriodeResilSansFrais','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] finAnciennePeriodeResilSansFrais_type_info = new String[]{'finAnciennePeriodeResilSansFrais','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] finNouvellePeriodeResilSansFrais_type_info = new String[]{'finNouvellePeriodeResilSansFrais','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateCreationEntete_type_info = new String[]{'dateCreationEntete','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] dateCreationPoste_type_info = new String[]{'dateCreationPoste','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] consommationAnnuelleEstimeeGaz_type_info = new String[]{'consommationAnnuelleEstimeeGaz','http://gdfsuez.com/bef/calypso/contrat','string40','0','1','false'};

        private String[] consommationAnnuelleEstimeeElec_type_info = new String[]{'consommationAnnuelleEstimeeElec','http://gdfsuez.com/bef/calypso/contrat','string40','0','1','false'};

        private String[] FTA_type_info = new String[]{'FTA','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] consommationAnnuelleEstimeeCadrans_type_info = new String[]{'consommationAnnuelleEstimeeCadrans','http://gdfsuez.com/bef/calypso/contrat','','0','1','false'};

        private String[] remiseRecurrente_type_info = new String[]{'remiseRecurrente','http://gdfsuez.com/bef/calypso/contrat','','0','-1','false'};

        private String[] remiseOneShot_type_info = new String[]{'remiseOneShot','http://gdfsuez.com/bef/calypso/contrat','RemiseOneShotDt','0','-1','false'};

        private String[] dateComposanteSupplementaire_type_info = new String[]{'dateComposanteSupplementaire','http://gdfsuez.com/bef/calypso/contrat','dateSupplementaire','0','-1','false'};

        private String[] offre_type_info = new String[]{'offre','http://gdfsuez.com/bef/calypso/contrat','OffreAbstract','0','1','false'};

        private String[] CARD_type_info = new String[]{'CARD','http://gdfsuez.com/bef/calypso/contrat','string','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'identifiantHistorique','typeComposante','clePrix','calendrierFournisseur','cadransCalendrierFournisseur','tarifAcheminement','puissance','versionUtilisation','profilConsommation','statutComposante','typeComptage','plageConsommation','consommationAnnuelleReference','nombreCertificatsVerts','motifResiliation','dateCreationHistorique','dateDemenagementSouhaitee','dateEmmenagementSouhaitee','dateDebutContrat','dateFinContrat','dureeHorsHistorique','dateFinPrevisionnelle','delaiResiliation','entreeResiliation','demandeResiliation','dateEffetResiliation','dateEffetContractuelleInitiale','dateEffetContractuelle','dateEcheance','dateProposition','debutAnciennePeriodeResilSansFrais','debutNouvellePeriodeResilSansFrais','finAnciennePeriodeResilSansFrais','finNouvellePeriodeResilSansFrais','dateCreationEntete','dateCreationPoste','consommationAnnuelleEstimeeGaz','consommationAnnuelleEstimeeElec','FTA','consommationAnnuelleEstimeeCadrans','remiseRecurrente','remiseOneShot','dateComposanteSupplementaire','offre','CARD'};

        public Contrat_composanteContrat_historiqueComposante_element(){

        }

        public Contrat_composanteContrat_historiqueComposante_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode identifiantHistoriqueNode = responseNode.getChildElement('identifiantHistorique', null);

            this.identifiantHistorique = (identifiantHistoriqueNode == null) ? null : identifiantHistoriqueNode.getText();

            nodesParsed.add(identifiantHistoriqueNode);

            //System.debug(this.identifiantHistorique);

            DOM.XmlNode typeComposanteNode = responseNode.getChildElement('typeComposante', null);

            if(typeComposanteNode == null){

                this.typeComposante = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt typeComposanteObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(typeComposanteNode);

                nodesParsed.add(typeComposanteNode);

                this.typeComposante = typeComposanteObj;

            }

            //System.debug(this.typeComposante);

            Dom.XmlNode[] clePrixChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_clePrix_element[] clePrixList = new List<gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_clePrix_element>();

            if(clePrixChildren == null || clePrixChildren.size() == 0){

                this.clePrix = null;

            } else{

                for(DOM.XmlNode childNode : clePrixChildren){

                    if(childNode.GetName() == 'clePrix'){

                        gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_clePrix_element Obj = new gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_clePrix_element(childNode);

                        clePrixList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.clePrix = clePrixList;

            }

            //System.debug(this.clePrix);

            DOM.XmlNode calendrierFournisseurNode = responseNode.getChildElement('calendrierFournisseur', 'http://gdfsuez.com/bef/calypso/contrat');

            if(calendrierFournisseurNode == null){

                this.calendrierFournisseur = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt calendrierFournisseurObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(calendrierFournisseurNode);

                nodesParsed.add(calendrierFournisseurNode);

                this.calendrierFournisseur = calendrierFournisseurObj;

            }

            //System.debug(this.calendrierFournisseur);

            DOM.XmlNode cadransCalendrierFournisseurNode = responseNode.getChildElement('cadransCalendrierFournisseur', 'http://gdfsuez.com/bef/calypso/contrat');

            if(cadransCalendrierFournisseurNode == null){

                this.cadransCalendrierFournisseur = null;

            } else{

                gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_cadransCalendrierFournisseur_element cadransCalendrierFournisseurObj = new gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_cadransCalendrierFournisseur_element(cadransCalendrierFournisseurNode);

                nodesParsed.add(cadransCalendrierFournisseurNode);

                this.cadransCalendrierFournisseur = cadransCalendrierFournisseurObj;

            }

            //System.debug(this.cadransCalendrierFournisseur);

            DOM.XmlNode tarifAcheminementNode = responseNode.getChildElement('tarifAcheminement', 'http://gdfsuez.com/bef/calypso/contrat');

            if(tarifAcheminementNode == null){

                this.tarifAcheminement = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt tarifAcheminementObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(tarifAcheminementNode);

                nodesParsed.add(tarifAcheminementNode);

                this.tarifAcheminement = tarifAcheminementObj;

            }

            //System.debug(this.tarifAcheminement);

            DOM.XmlNode puissanceNode = responseNode.getChildElement('puissance', 'http://gdfsuez.com/bef/calypso/contrat');

            if(puissanceNode == null){

                this.puissance = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt puissanceObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(puissanceNode);

                nodesParsed.add(puissanceNode);

                this.puissance = puissanceObj;

            }

            //System.debug(this.puissance);

            DOM.XmlNode versionUtilisationNode = responseNode.getChildElement('versionUtilisation', 'http://gdfsuez.com/bef/calypso/contrat');

            if(versionUtilisationNode == null){

                this.versionUtilisation = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt versionUtilisationObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(versionUtilisationNode);

                nodesParsed.add(versionUtilisationNode);

                this.versionUtilisation = versionUtilisationObj;

            }

            //System.debug(this.versionUtilisation);

            DOM.XmlNode profilConsommationNode = responseNode.getChildElement('profilConsommation', 'http://gdfsuez.com/bef/calypso/contrat');

            if(profilConsommationNode == null){

                this.profilConsommation = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt profilConsommationObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(profilConsommationNode);

                nodesParsed.add(profilConsommationNode);

                this.profilConsommation = profilConsommationObj;

            }

            //System.debug(this.profilConsommation);

            DOM.XmlNode statutComposanteNode = responseNode.getChildElement('statutComposante', null);

            if(statutComposanteNode == null){

                this.statutComposante = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt statutComposanteObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(statutComposanteNode);

                nodesParsed.add(statutComposanteNode);

                this.statutComposante = statutComposanteObj;

            }

            //System.debug(this.statutComposante);

            DOM.XmlNode typeComptageNode = responseNode.getChildElement('typeComptage', 'http://gdfsuez.com/bef/calypso/contrat');

            if(typeComptageNode == null){

                this.typeComptage = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt typeComptageObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(typeComptageNode);

                nodesParsed.add(typeComptageNode);

                this.typeComptage = typeComptageObj;

            }

            //System.debug(this.typeComptage);

            DOM.XmlNode plageConsommationNode = responseNode.getChildElement('plageConsommation', 'http://gdfsuez.com/bef/calypso/contrat');

            if(plageConsommationNode == null){

                this.plageConsommation = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt plageConsommationObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(plageConsommationNode);

                nodesParsed.add(plageConsommationNode);

                this.plageConsommation = plageConsommationObj;

            }

            //System.debug(this.plageConsommation);

            DOM.XmlNode consommationAnnuelleReferenceNode = responseNode.getChildElement('consommationAnnuelleReference', 'http://gdfsuez.com/bef/calypso/contrat');

            this.consommationAnnuelleReference = (consommationAnnuelleReferenceNode == null) ? null : consommationAnnuelleReferenceNode.getText();

            nodesParsed.add(consommationAnnuelleReferenceNode);

            //System.debug(this.consommationAnnuelleReference);

            DOM.XmlNode nombreCertificatsVertsNode = responseNode.getChildElement('nombreCertificatsVerts', 'http://gdfsuez.com/bef/calypso/contrat');

            this.nombreCertificatsVerts = (nombreCertificatsVertsNode == null) ? null : nombreCertificatsVertsNode.getText();

            nodesParsed.add(nombreCertificatsVertsNode);

            //System.debug(this.nombreCertificatsVerts);

            DOM.XmlNode motifResiliationNode = responseNode.getChildElement('motifResiliation', 'http://gdfsuez.com/bef/calypso/contrat');

            if(motifResiliationNode == null){

                this.motifResiliation = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt motifResiliationObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(motifResiliationNode);

                nodesParsed.add(motifResiliationNode);

                this.motifResiliation = motifResiliationObj;

           }

            //System.debug(this.motifResiliation);

            DOM.XmlNode dateCreationHistoriqueNode = responseNode.getChildElement('dateCreationHistorique', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dateCreationHistorique = (dateCreationHistoriqueNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateCreationHistoriqueNode.getText());

            nodesParsed.add(dateCreationHistoriqueNode);

            //System.debug(this.dateCreationHistorique);

            DOM.XmlNode dateDemenagementSouhaiteeNode = responseNode.getChildElement('dateDemenagementSouhaitee', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dateDemenagementSouhaitee = (dateDemenagementSouhaiteeNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateDemenagementSouhaiteeNode.getText());

            nodesParsed.add(dateDemenagementSouhaiteeNode);

            //System.debug(this.dateDemenagementSouhaitee);

            DOM.XmlNode dateEmmenagementSouhaiteeNode = responseNode.getChildElement('dateEmmenagementSouhaitee', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dateEmmenagementSouhaitee = (dateEmmenagementSouhaiteeNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateEmmenagementSouhaiteeNode.getText());

            nodesParsed.add(dateEmmenagementSouhaiteeNode);

            //System.debug(this.dateEmmenagementSouhaitee);

            DOM.XmlNode dateDebutContratNode = responseNode.getChildElement('dateDebutContrat', null);

            this.dateDebutContrat = (dateDebutContratNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateDebutContratNode.getText());

            nodesParsed.add(dateDebutContratNode);

            //System.debug(this.dateDebutContrat);

            DOM.XmlNode dateFinContratNode = responseNode.getChildElement('dateFinContrat', null);

            this.dateFinContrat = (dateFinContratNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateFinContratNode.getText());

            nodesParsed.add(dateFinContratNode);

            //System.debug(this.dateFinContrat);

            DOM.XmlNode dureeHorsHistoriqueNode = responseNode.getChildElement('dureeHorsHistorique', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dureeHorsHistorique = (dureeHorsHistoriqueNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dureeHorsHistoriqueNode.getText());

            nodesParsed.add(dureeHorsHistoriqueNode);

            //System.debug(this.dureeHorsHistorique);

            DOM.XmlNode dateFinPrevisionnelleNode = responseNode.getChildElement('dateFinPrevisionnelle', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dateFinPrevisionnelle = (dateFinPrevisionnelleNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateFinPrevisionnelleNode.getText());

            nodesParsed.add(dateFinPrevisionnelleNode);

            //System.debug(this.dateFinPrevisionnelle);

            DOM.XmlNode delaiResiliationNode = responseNode.getChildElement('delaiResiliation', 'http://gdfsuez.com/bef/calypso/contrat');

            this.delaiResiliation = (delaiResiliationNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(delaiResiliationNode.getText());

            nodesParsed.add(delaiResiliationNode);

            //System.debug(this.delaiResiliation);

            DOM.XmlNode entreeResiliationNode = responseNode.getChildElement('entreeResiliation', 'http://gdfsuez.com/bef/calypso/contrat');

            this.entreeResiliation = (entreeResiliationNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(entreeResiliationNode.getText());

            nodesParsed.add(entreeResiliationNode);

            //System.debug(this.entreeResiliation);

            DOM.XmlNode demandeResiliationNode = responseNode.getChildElement('demandeResiliation', 'http://gdfsuez.com/bef/calypso/contrat');

            this.demandeResiliation = (demandeResiliationNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(demandeResiliationNode.getText());

            nodesParsed.add(demandeResiliationNode);

            //System.debug(this.demandeResiliation);

            DOM.XmlNode dateEffetResiliationNode = responseNode.getChildElement('dateEffetResiliation', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dateEffetResiliation = (dateEffetResiliationNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateEffetResiliationNode.getText());

            nodesParsed.add(dateEffetResiliationNode);

            //System.debug(this.dateEffetResiliation);

            DOM.XmlNode dateEffetContractuelleInitialeNode = responseNode.getChildElement('dateEffetContractuelleInitiale', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dateEffetContractuelleInitiale = (dateEffetContractuelleInitialeNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateEffetContractuelleInitialeNode.getText());

            nodesParsed.add(dateEffetContractuelleInitialeNode);

            //System.debug(this.dateEffetContractuelleInitiale);

            DOM.XmlNode dateEffetContractuelleNode = responseNode.getChildElement('dateEffetContractuelle', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dateEffetContractuelle = (dateEffetContractuelleNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateEffetContractuelleNode.getText());

            nodesParsed.add(dateEffetContractuelleNode);

            //System.debug(this.dateEffetContractuelle);

            DOM.XmlNode dateEcheanceNode = responseNode.getChildElement('dateEcheance', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dateEcheance = (dateEcheanceNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateEcheanceNode.getText());

            nodesParsed.add(dateEcheanceNode);

            //System.debug(this.dateEcheance);

            DOM.XmlNode datePropositionNode = responseNode.getChildElement('dateProposition', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dateProposition = (datePropositionNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(datePropositionNode.getText());

            nodesParsed.add(datePropositionNode);

            //System.debug(this.dateProposition);

            DOM.XmlNode debutAnciennePeriodeResilSansFraisNode = responseNode.getChildElement('debutAnciennePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat');

            this.debutAnciennePeriodeResilSansFrais = (debutAnciennePeriodeResilSansFraisNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(debutAnciennePeriodeResilSansFraisNode.getText());

            nodesParsed.add(debutAnciennePeriodeResilSansFraisNode);

            //System.debug(this.debutAnciennePeriodeResilSansFrais);

            DOM.XmlNode debutNouvellePeriodeResilSansFraisNode = responseNode.getChildElement('debutNouvellePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat');

            this.debutNouvellePeriodeResilSansFrais = (debutNouvellePeriodeResilSansFraisNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(debutNouvellePeriodeResilSansFraisNode.getText());

            nodesParsed.add(debutNouvellePeriodeResilSansFraisNode);

            //System.debug(this.debutNouvellePeriodeResilSansFrais);

            DOM.XmlNode finAnciennePeriodeResilSansFraisNode = responseNode.getChildElement('finAnciennePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat');

            this.finAnciennePeriodeResilSansFrais = (finAnciennePeriodeResilSansFraisNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(finAnciennePeriodeResilSansFraisNode.getText());

            nodesParsed.add(finAnciennePeriodeResilSansFraisNode);

            //System.debug(this.finAnciennePeriodeResilSansFrais);

            DOM.XmlNode finNouvellePeriodeResilSansFraisNode = responseNode.getChildElement('finNouvellePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat');

            this.finNouvellePeriodeResilSansFrais = (finNouvellePeriodeResilSansFraisNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(finNouvellePeriodeResilSansFraisNode.getText());

            nodesParsed.add(finNouvellePeriodeResilSansFraisNode);

            //System.debug(this.finNouvellePeriodeResilSansFrais);

            DOM.XmlNode dateCreationEnteteNode = responseNode.getChildElement('dateCreationEntete', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dateCreationEntete = (dateCreationEnteteNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateCreationEnteteNode.getText());

            nodesParsed.add(dateCreationEnteteNode);

            //System.debug(this.dateCreationEntete);

            DOM.XmlNode dateCreationPosteNode = responseNode.getChildElement('dateCreationPoste', 'http://gdfsuez.com/bef/calypso/contrat');

            this.dateCreationPoste = (dateCreationPosteNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(dateCreationPosteNode.getText());

            nodesParsed.add(dateCreationPosteNode);

            //System.debug(this.dateCreationPoste);

            DOM.XmlNode consommationAnnuelleEstimeeGazNode = responseNode.getChildElement('consommationAnnuelleEstimeeGaz', 'http://gdfsuez.com/bef/calypso/contrat');

            this.consommationAnnuelleEstimeeGaz = (consommationAnnuelleEstimeeGazNode == null) ? null : consommationAnnuelleEstimeeGazNode.getText();

            nodesParsed.add(consommationAnnuelleEstimeeGazNode);

            //System.debug(this.consommationAnnuelleEstimeeGaz);

            DOM.XmlNode consommationAnnuelleEstimeeElecNode = responseNode.getChildElement('consommationAnnuelleEstimeeElec', 'http://gdfsuez.com/bef/calypso/contrat');

            this.consommationAnnuelleEstimeeElec = (consommationAnnuelleEstimeeElecNode == null) ? null : consommationAnnuelleEstimeeElecNode.getText();

            nodesParsed.add(consommationAnnuelleEstimeeElecNode);

            //System.debug(this.consommationAnnuelleEstimeeElec);

            DOM.XmlNode FTANode = responseNode.getChildElement('FTA', 'http://gdfsuez.com/bef/calypso/contrat');

            if(FTANode == null){

                this.FTA = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt FTAObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(FTANode);

                nodesParsed.add(FTANode);

                this.FTA = FTAObj;

            }

            //System.debug(this.FTA);

            DOM.XmlNode consommationAnnuelleEstimeeCadransNode = responseNode.getChildElement('consommationAnnuelleEstimeeCadrans', 'http://gdfsuez.com/bef/calypso/contrat');

            if(consommationAnnuelleEstimeeCadransNode == null){

                this.consommationAnnuelleEstimeeCadrans = null;

            } else{

                gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_element consommationAnnuelleEstimeeCadransObj = new gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_element(consommationAnnuelleEstimeeCadransNode);

                nodesParsed.add(consommationAnnuelleEstimeeCadransNode);

                this.consommationAnnuelleEstimeeCadrans = consommationAnnuelleEstimeeCadransObj;

            }

            //System.debug(this.consommationAnnuelleEstimeeCadrans);

            Dom.XmlNode[] remiseRecurrenteChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_remiseRecurrente_element[] remiseRecurrenteList = new List<gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_remiseRecurrente_element>();

            if(remiseRecurrenteChildren == null || remiseRecurrenteChildren.size() == 0){

                this.remiseRecurrente = null;

            } else{

                for(DOM.XmlNode childNode : remiseRecurrenteChildren){

                    if(childNode.GetName() == 'remiseRecurrente'){

                        gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_remiseRecurrente_element Obj = new gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_remiseRecurrente_element(childNode);

                        remiseRecurrenteList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.remiseRecurrente = remiseRecurrenteList;

            }

            //System.debug(this.remiseRecurrente);

            Dom.XmlNode[] remiseOneShotChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.RemiseOneShotDt[] remiseOneShotList = new List<gdfsuezComBefCalypsoContrat.RemiseOneShotDt>();

            if(remiseOneShotChildren == null || remiseOneShotChildren.size() == 0){

                this.remiseOneShot = null;

            } else{

                for(DOM.XmlNode childNode : remiseOneShotChildren){

                    if(childNode.GetName() == 'remiseOneShot'){

                        gdfsuezComBefCalypsoContrat.RemiseOneShotDt Obj = new gdfsuezComBefCalypsoContrat.RemiseOneShotDt(childNode);

                        remiseOneShotList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.remiseOneShot = remiseOneShotList;

            }

            //System.debug(this.remiseOneShot);

            Dom.XmlNode[] dateComposanteSupplementaireChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.dateSupplementaire[] dateComposanteSupplementaireList = new List<gdfsuezComBefCalypsoContrat.dateSupplementaire>();

            if(dateComposanteSupplementaireChildren == null || dateComposanteSupplementaireChildren.size() == 0){

                this.dateComposanteSupplementaire = null;

            } else{

                for(DOM.XmlNode childNode : dateComposanteSupplementaireChildren){

                    if(childNode.GetName() == 'dateComposanteSupplementaire'){

                        gdfsuezComBefCalypsoContrat.dateSupplementaire Obj = new gdfsuezComBefCalypsoContrat.dateSupplementaire(childNode);

                        dateComposanteSupplementaireList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.dateComposanteSupplementaire = dateComposanteSupplementaireList;

            }

            //System.debug(this.dateComposanteSupplementaire);

            DOM.XmlNode offreNode = responseNode.getChildElement('offre', null);

            if(offreNode == null){

                this.offre = null;

            } else{

                gdfsuezComBefCalypsoContrat.OffreAbstract offreObj = new gdfsuezComBefCalypsoContrat.OffreAbstract(offreNode);

                nodesParsed.add(offreNode);

                this.offre = offreObj;

            }

            //System.debug(this.offre);

            DOM.XmlNode CARDNode = responseNode.getChildElement('CARD', 'http://gdfsuez.com/bef/calypso/contrat');

            this.CARD = (CARDNode == null) ? null : CARDNode.getText();

            nodesParsed.add(CARDNode);

            //System.debug(this.CARD);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode identifiantHistoriqueNode = outerNode.addChildElement('identifiantHistorique', 'http://gdfsuez.com/bef/calypso/contrat', '');

            identifiantHistoriqueNode.addTextNode((this.identifiantHistorique == null) ? '' : this.identifiantHistorique);



            gdfsuezComBefChpPivotContrat.listeValeursDt typeComposanteObj = this.typeComposante;

            Dom.XmlNode typeComposanteNode = outerNode.addChildElement('typeComposante', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(typeComposanteObj != null){

                typeComposanteObj.populateXmlNode(typeComposanteNode);

            }



            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_clePrix_element[] clePrixObj = this.clePrix;

            if(clePrixObj != null){

                for(gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_clePrix_element element: clePrixObj){

                    Dom.XmlNode clePrixNode = outerNode.addChildElement('clePrix', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(clePrixNode);

                }

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt calendrierFournisseurObj = this.calendrierFournisseur;

            Dom.XmlNode calendrierFournisseurNode = outerNode.addChildElement('calendrierFournisseur', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(calendrierFournisseurObj != null){

                calendrierFournisseurObj.populateXmlNode(calendrierFournisseurNode);

            }



            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_cadransCalendrierFournisseur_element cadransCalendrierFournisseurObj = this.cadransCalendrierFournisseur;

            Dom.XmlNode cadransCalendrierFournisseurNode = outerNode.addChildElement('cadransCalendrierFournisseur', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(cadransCalendrierFournisseurObj != null){

                cadransCalendrierFournisseurObj.populateXmlNode(cadransCalendrierFournisseurNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt tarifAcheminementObj = this.tarifAcheminement;

            Dom.XmlNode tarifAcheminementNode = outerNode.addChildElement('tarifAcheminement', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(tarifAcheminementObj != null){

                tarifAcheminementObj.populateXmlNode(tarifAcheminementNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt puissanceObj = this.puissance;

            Dom.XmlNode puissanceNode = outerNode.addChildElement('puissance', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(puissanceObj != null){

                puissanceObj.populateXmlNode(puissanceNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt versionUtilisationObj = this.versionUtilisation;

            Dom.XmlNode versionUtilisationNode = outerNode.addChildElement('versionUtilisation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(versionUtilisationObj != null){

                versionUtilisationObj.populateXmlNode(versionUtilisationNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt profilConsommationObj = this.profilConsommation;

            Dom.XmlNode profilConsommationNode = outerNode.addChildElement('profilConsommation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(profilConsommationObj != null){

                profilConsommationObj.populateXmlNode(profilConsommationNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt statutComposanteObj = this.statutComposante;

            Dom.XmlNode statutComposanteNode = outerNode.addChildElement('statutComposante', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(statutComposanteObj != null){

                statutComposanteObj.populateXmlNode(statutComposanteNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt typeComptageObj = this.typeComptage;

            Dom.XmlNode typeComptageNode = outerNode.addChildElement('typeComptage', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(typeComptageObj != null){

                typeComptageObj.populateXmlNode(typeComptageNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt plageConsommationObj = this.plageConsommation;

            Dom.XmlNode plageConsommationNode = outerNode.addChildElement('plageConsommation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(plageConsommationObj != null){

                plageConsommationObj.populateXmlNode(plageConsommationNode);

            }

            Dom.XmlNode consommationAnnuelleReferenceNode = outerNode.addChildElement('consommationAnnuelleReference', 'http://gdfsuez.com/bef/calypso/contrat', '');

            consommationAnnuelleReferenceNode.addTextNode((this.consommationAnnuelleReference == null) ? '' : this.consommationAnnuelleReference);

            Dom.XmlNode nombreCertificatsVertsNode = outerNode.addChildElement('nombreCertificatsVerts', 'http://gdfsuez.com/bef/calypso/contrat', '');

            nombreCertificatsVertsNode.addTextNode((this.nombreCertificatsVerts == null) ? '' : this.nombreCertificatsVerts);



            gdfsuezComBefChpPivotContrat.listeValeursDt motifResiliationObj = this.motifResiliation;

            Dom.XmlNode motifResiliationNode = outerNode.addChildElement('motifResiliation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(motifResiliationObj != null){

                motifResiliationObj.populateXmlNode(motifResiliationNode);

            }

            Dom.XmlNode dateCreationHistoriqueNode = outerNode.addChildElement('dateCreationHistorique', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateCreationHistoriqueNode.addTextNode((this.dateCreationHistorique == null) ? '' : String.valueOf(this.dateCreationHistorique));

            Dom.XmlNode dateDemenagementSouhaiteeNode = outerNode.addChildElement('dateDemenagementSouhaitee', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateDemenagementSouhaiteeNode.addTextNode((this.dateDemenagementSouhaitee == null) ? '' : String.valueOf(this.dateDemenagementSouhaitee));

            Dom.XmlNode dateEmmenagementSouhaiteeNode = outerNode.addChildElement('dateEmmenagementSouhaitee', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateEmmenagementSouhaiteeNode.addTextNode((this.dateEmmenagementSouhaitee == null) ? '' : String.valueOf(this.dateEmmenagementSouhaitee));

            Dom.XmlNode dateDebutContratNode = outerNode.addChildElement('dateDebutContrat', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateDebutContratNode.addTextNode((this.dateDebutContrat == null) ? '' : String.valueOf(this.dateDebutContrat));

            Dom.XmlNode dateFinContratNode = outerNode.addChildElement('dateFinContrat', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateFinContratNode.addTextNode((this.dateFinContrat == null) ? '' : String.valueOf(this.dateFinContrat));

            Dom.XmlNode dureeHorsHistoriqueNode = outerNode.addChildElement('dureeHorsHistorique', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dureeHorsHistoriqueNode.addTextNode((this.dureeHorsHistorique == null) ? '' : String.valueOf(this.dureeHorsHistorique));

            Dom.XmlNode dateFinPrevisionnelleNode = outerNode.addChildElement('dateFinPrevisionnelle', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateFinPrevisionnelleNode.addTextNode((this.dateFinPrevisionnelle == null) ? '' : String.valueOf(this.dateFinPrevisionnelle));

            Dom.XmlNode delaiResiliationNode = outerNode.addChildElement('delaiResiliation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            delaiResiliationNode.addTextNode((this.delaiResiliation == null) ? '' : String.valueOf(this.delaiResiliation));

            Dom.XmlNode entreeResiliationNode = outerNode.addChildElement('entreeResiliation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            entreeResiliationNode.addTextNode((this.entreeResiliation == null) ? '' : String.valueOf(this.entreeResiliation));

            Dom.XmlNode demandeResiliationNode = outerNode.addChildElement('demandeResiliation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            demandeResiliationNode.addTextNode((this.demandeResiliation == null) ? '' : String.valueOf(this.demandeResiliation));

            Dom.XmlNode dateEffetResiliationNode = outerNode.addChildElement('dateEffetResiliation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateEffetResiliationNode.addTextNode((this.dateEffetResiliation == null) ? '' : String.valueOf(this.dateEffetResiliation));

            Dom.XmlNode dateEffetContractuelleInitialeNode = outerNode.addChildElement('dateEffetContractuelleInitiale', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateEffetContractuelleInitialeNode.addTextNode((this.dateEffetContractuelleInitiale == null) ? '' : String.valueOf(this.dateEffetContractuelleInitiale));

            Dom.XmlNode dateEffetContractuelleNode = outerNode.addChildElement('dateEffetContractuelle', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateEffetContractuelleNode.addTextNode((this.dateEffetContractuelle == null) ? '' : String.valueOf(this.dateEffetContractuelle));

            Dom.XmlNode dateEcheanceNode = outerNode.addChildElement('dateEcheance', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateEcheanceNode.addTextNode((this.dateEcheance == null) ? '' : String.valueOf(this.dateEcheance));

            Dom.XmlNode datePropositionNode = outerNode.addChildElement('dateProposition', 'http://gdfsuez.com/bef/calypso/contrat', '');

            datePropositionNode.addTextNode((this.dateProposition == null) ? '' : String.valueOf(this.dateProposition));

            Dom.XmlNode debutAnciennePeriodeResilSansFraisNode = outerNode.addChildElement('debutAnciennePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat', '');

            debutAnciennePeriodeResilSansFraisNode.addTextNode((this.debutAnciennePeriodeResilSansFrais == null) ? '' : String.valueOf(this.debutAnciennePeriodeResilSansFrais));

            Dom.XmlNode debutNouvellePeriodeResilSansFraisNode = outerNode.addChildElement('debutNouvellePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat', '');

            debutNouvellePeriodeResilSansFraisNode.addTextNode((this.debutNouvellePeriodeResilSansFrais == null) ? '' : String.valueOf(this.debutNouvellePeriodeResilSansFrais));

            Dom.XmlNode finAnciennePeriodeResilSansFraisNode = outerNode.addChildElement('finAnciennePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat', '');

            finAnciennePeriodeResilSansFraisNode.addTextNode((this.finAnciennePeriodeResilSansFrais == null) ? '' : String.valueOf(this.finAnciennePeriodeResilSansFrais));

            Dom.XmlNode finNouvellePeriodeResilSansFraisNode = outerNode.addChildElement('finNouvellePeriodeResilSansFrais', 'http://gdfsuez.com/bef/calypso/contrat', '');

            finNouvellePeriodeResilSansFraisNode.addTextNode((this.finNouvellePeriodeResilSansFrais == null) ? '' : String.valueOf(this.finNouvellePeriodeResilSansFrais));

            Dom.XmlNode dateCreationEnteteNode = outerNode.addChildElement('dateCreationEntete', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateCreationEnteteNode.addTextNode((this.dateCreationEntete == null) ? '' : String.valueOf(this.dateCreationEntete));

            Dom.XmlNode dateCreationPosteNode = outerNode.addChildElement('dateCreationPoste', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateCreationPosteNode.addTextNode((this.dateCreationPoste == null) ? '' : String.valueOf(this.dateCreationPoste));

            Dom.XmlNode consommationAnnuelleEstimeeGazNode = outerNode.addChildElement('consommationAnnuelleEstimeeGaz', 'http://gdfsuez.com/bef/calypso/contrat', '');

            consommationAnnuelleEstimeeGazNode.addTextNode((this.consommationAnnuelleEstimeeGaz == null) ? '' : this.consommationAnnuelleEstimeeGaz);

            Dom.XmlNode consommationAnnuelleEstimeeElecNode = outerNode.addChildElement('consommationAnnuelleEstimeeElec', 'http://gdfsuez.com/bef/calypso/contrat', '');

            consommationAnnuelleEstimeeElecNode.addTextNode((this.consommationAnnuelleEstimeeElec == null) ? '' : this.consommationAnnuelleEstimeeElec);



            gdfsuezComBefChpPivotContrat.listeValeursDt FTAObj = this.FTA;

            Dom.XmlNode FTANode = outerNode.addChildElement('FTA', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(FTAObj != null){

                FTAObj.populateXmlNode(FTANode);

            }



            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_consommationAnnuelleEstimeeCadrans_element consommationAnnuelleEstimeeCadransObj = this.consommationAnnuelleEstimeeCadrans;

            Dom.XmlNode consommationAnnuelleEstimeeCadransNode = outerNode.addChildElement('consommationAnnuelleEstimeeCadrans', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(consommationAnnuelleEstimeeCadransObj != null){

                consommationAnnuelleEstimeeCadransObj.populateXmlNode(consommationAnnuelleEstimeeCadransNode);

            }



            gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_remiseRecurrente_element[] remiseRecurrenteObj = this.remiseRecurrente;

            if(remiseRecurrenteObj != null){

                for(gdfsuezComBefCalypsoContrat.Contrat_composanteContrat_historiqueComposante_remiseRecurrente_element element: remiseRecurrenteObj){

                    Dom.XmlNode remiseRecurrenteNode = outerNode.addChildElement('remiseRecurrente', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(remiseRecurrenteNode);

                }

            }



            gdfsuezComBefCalypsoContrat.RemiseOneShotDt[] remiseOneShotObj = this.remiseOneShot;

            if(remiseOneShotObj != null){

                for(gdfsuezComBefCalypsoContrat.RemiseOneShotDt element: remiseOneShotObj){

                    Dom.XmlNode remiseOneShotNode = outerNode.addChildElement('remiseOneShot', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(remiseOneShotNode);

                }

            }



            gdfsuezComBefCalypsoContrat.dateSupplementaire[] dateComposanteSupplementaireObj = this.dateComposanteSupplementaire;

            if(dateComposanteSupplementaireObj != null){

                for(gdfsuezComBefCalypsoContrat.dateSupplementaire element: dateComposanteSupplementaireObj){

                    Dom.XmlNode dateComposanteSupplementaireNode = outerNode.addChildElement('dateComposanteSupplementaire', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(dateComposanteSupplementaireNode);

                }

            }



            gdfsuezComBefCalypsoContrat.OffreAbstract offreObj = this.offre;

            Dom.XmlNode offreNode = outerNode.addChildElement('offre', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(offreObj != null){

                offreObj.populateXmlNode(offreNode);

            }

            Dom.XmlNode CARDNode = outerNode.addChildElement('CARD', 'http://gdfsuez.com/bef/calypso/contrat', '');

            CARDNode.addTextNode((this.CARD == null) ? '' : this.CARD);

        }

    }

    public class Contrat_composanteContrat_historiqueComposante_remiseRecurrente_element {

        public gdfsuezComBefChpPivotContrat.listeValeursDt typeRemiseReccurente;

        public String cleRemise;

        public Double pourcentageRemise;

        public String quantite;

        public String duree;

        private String[] typeRemiseReccurente_type_info = new String[]{'typeRemiseReccurente','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] cleRemise_type_info = new String[]{'cleRemise','http://gdfsuez.com/bef/calypso/contrat','string20','0','1','false'};

        private String[] pourcentageRemise_type_info = new String[]{'pourcentageRemise','http://gdfsuez.com/bef/calypso/contrat','float','0','1','false'};

       private String[] quantite_type_info = new String[]{'quantite','http://gdfsuez.com/bef/calypso/contrat','string40','0','1','false'};

        private String[] duree_type_info = new String[]{'duree','http://gdfsuez.com/bef/calypso/contrat','string5','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'typeRemiseReccurente','cleRemise','pourcentageRemise','quantite','duree'};

        public Contrat_composanteContrat_historiqueComposante_remiseRecurrente_element(){

        }

        public Contrat_composanteContrat_historiqueComposante_remiseRecurrente_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode typeRemiseReccurenteNode = responseNode.getChildElement('typeRemiseReccurente', null);

            if(typeRemiseReccurenteNode == null){

                this.typeRemiseReccurente = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt typeRemiseReccurenteObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(typeRemiseReccurenteNode);

                nodesParsed.add(typeRemiseReccurenteNode);

                this.typeRemiseReccurente = typeRemiseReccurenteObj;

            }

            //System.debug(this.typeRemiseReccurente);

            DOM.XmlNode cleRemiseNode = responseNode.getChildElement('cleRemise', null);

            this.cleRemise = (cleRemiseNode == null) ? null : cleRemiseNode.getText();

            nodesParsed.add(cleRemiseNode);

            //System.debug(this.cleRemise);

            DOM.XmlNode pourcentageRemiseNode = responseNode.getChildElement('pourcentageRemise', null);

            this.pourcentageRemise = (pourcentageRemiseNode == null) ? null : Double.valueOf(pourcentageRemiseNode.getText());

            nodesParsed.add(pourcentageRemiseNode);

            //System.debug(this.pourcentageRemise);

            DOM.XmlNode quantiteNode = responseNode.getChildElement('quantite', null);

            this.quantite = (quantiteNode == null) ? null : quantiteNode.getText();

            nodesParsed.add(quantiteNode);

            //System.debug(this.quantite);

            DOM.XmlNode dureeNode = responseNode.getChildElement('duree', null);

            this.duree = (dureeNode == null) ? null : dureeNode.getText();

            nodesParsed.add(dureeNode);

            //System.debug(this.duree);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){



            gdfsuezComBefChpPivotContrat.listeValeursDt typeRemiseReccurenteObj = this.typeRemiseReccurente;

            Dom.XmlNode typeRemiseReccurenteNode = outerNode.addChildElement('typeRemiseReccurente', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(typeRemiseReccurenteObj != null){

                typeRemiseReccurenteObj.populateXmlNode(typeRemiseReccurenteNode);

            }

            Dom.XmlNode cleRemiseNode = outerNode.addChildElement('cleRemise', 'http://gdfsuez.com/bef/calypso/contrat', '');

            cleRemiseNode.addTextNode((this.cleRemise == null) ? '' : this.cleRemise);

            Dom.XmlNode pourcentageRemiseNode = outerNode.addChildElement('pourcentageRemise', 'http://gdfsuez.com/bef/calypso/contrat', '');

            pourcentageRemiseNode.addTextNode((this.pourcentageRemise == null) ? '' : String.valueOf(this.pourcentageRemise));

            Dom.XmlNode quantiteNode = outerNode.addChildElement('quantite', 'http://gdfsuez.com/bef/calypso/contrat', '');

            quantiteNode.addTextNode((this.quantite == null) ? '' : this.quantite);

            Dom.XmlNode dureeNode = outerNode.addChildElement('duree', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dureeNode.addTextNode((this.duree == null) ? '' : this.duree);

        }

    }

    public class Contrat_objetsLies_element {

        public String idObjetLie;

        public String typeObjetLie;

        private String[] idObjetLie_type_info = new String[]{'idObjetLie','http://gdfsuez.com/bef/calypso/contrat','string50','0','1','false'};

        private String[] typeObjetLie_type_info = new String[]{'typeObjetLie','http://gdfsuez.com/bef/calypso/contrat','string50','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'idObjetLie','typeObjetLie'};

        public Contrat_objetsLies_element(){

        }

        public Contrat_objetsLies_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode idObjetLieNode = responseNode.getChildElement('idObjetLie', null);

            this.idObjetLie = (idObjetLieNode == null) ? null : idObjetLieNode.getText();

            nodesParsed.add(idObjetLieNode);

            //System.debug(this.idObjetLie);

            DOM.XmlNode typeObjetLieNode = responseNode.getChildElement('typeObjetLie', null);

            this.typeObjetLie = (typeObjetLieNode == null) ? null : typeObjetLieNode.getText();

            nodesParsed.add(typeObjetLieNode);

            //System.debug(this.typeObjetLie);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode idObjetLieNode = outerNode.addChildElement('idObjetLie', 'http://gdfsuez.com/bef/calypso/contrat', '');

            idObjetLieNode.addTextNode((this.idObjetLie == null) ? '' : this.idObjetLie);

            Dom.XmlNode typeObjetLieNode = outerNode.addChildElement('typeObjetLie', 'http://gdfsuez.com/bef/calypso/contrat', '');

            typeObjetLieNode.addTextNode((this.typeObjetLie == null) ? '' : this.typeObjetLie);

        }

    }

    public class Contrat_rolePersonne_element {

        public gdfsuezComBefCalypsoContrat.PersonneAbstract personne;

        public gdfsuezComBefChpPivotContrat.listeValeursDt rolePersonne;

        private String[] personne_type_info = new String[]{'personne','http://gdfsuez.com/bef/calypso/contrat','PersonneAbstract','0','1','false'};

        private String[] rolePersonne_type_info = new String[]{'rolePersonne','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'personne','rolePersonne'};

        public Contrat_rolePersonne_element(){

        }

        public Contrat_rolePersonne_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode personneNode = responseNode.getChildElement('personne', null);

            if(personneNode == null){

                this.personne = null;

            } else{

                gdfsuezComBefCalypsoContrat.PersonneAbstract personneObj = new gdfsuezComBefCalypsoContrat.PersonneAbstract(personneNode);

                nodesParsed.add(personneNode);

                this.personne = personneObj;

            }

            //System.debug(this.personne);

            DOM.XmlNode rolePersonneNode = responseNode.getChildElement('rolePersonne', null);

            if(rolePersonneNode == null){

                this.rolePersonne = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt rolePersonneObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(rolePersonneNode);

                nodesParsed.add(rolePersonneNode);

                this.rolePersonne = rolePersonneObj;

            }

            //System.debug(this.rolePersonne);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){



            gdfsuezComBefCalypsoContrat.PersonneAbstract personneObj = this.personne;

            Dom.XmlNode personneNode = outerNode.addChildElement('personne', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(personneObj != null){

                personneObj.populateXmlNode(personneNode);

            }



            gdfsuezComBefChpPivotContrat.listeValeursDt rolePersonneObj = this.rolePersonne;

            Dom.XmlNode rolePersonneNode = outerNode.addChildElement('rolePersonne', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(rolePersonneObj != null){

                rolePersonneObj.populateXmlNode(rolePersonneNode);

            }

        }

    }

    public class Contrat_TSE_element {

        public gdfsuezComBefChpPivotContrat.listeValeursDt typeTSE;

        public Boolean TSEActif;

        public Date dateDebut;

        public Date dateFin;

        public String uniteConsommation;

        public String remiseAnnuelleHT;

        private String[] typeTSE_type_info = new String[]{'typeTSE','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] TSEActif_type_info = new String[]{'TSEActif','http://gdfsuez.com/bef/calypso/contrat','boolean','0','1','false'};

        private String[] dateDebut_type_info = new String[]{'dateDebut','http://gdfsuez.com/bef/calypso/contrat','date','0','1','false'};

        private String[] dateFin_type_info = new String[]{'dateFin','http://gdfsuez.com/bef/calypso/contrat','date','0','1','false'};

        private String[] uniteConsommation_type_info = new String[]{'uniteConsommation','http://gdfsuez.com/bef/calypso/contrat','string','0','1','false'};

        private String[] remiseAnnuelleHT_type_info = new String[]{'remiseAnnuelleHT','http://gdfsuez.com/bef/calypso/contrat','string10','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'typeTSE','TSEActif','dateDebut','dateFin','uniteConsommation','remiseAnnuelleHT'};

        public Contrat_TSE_element(){

        }

        public Contrat_TSE_element(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode typeTSENode = responseNode.getChildElement('typeTSE', null);

            if(typeTSENode == null){

                this.typeTSE = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt typeTSEObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(typeTSENode);

                nodesParsed.add(typeTSENode);

                this.typeTSE = typeTSEObj;

            }

            //System.debug(this.typeTSE);

            DOM.XmlNode TSEActifNode = responseNode.getChildElement('TSEActif', null);

            this.TSEActif = (TSEActifNode == null) ? null : Boolean.valueOf(TSEActifNode.getText());

            nodesParsed.add(TSEActifNode);

            //System.debug(this.TSEActif);

            DOM.XmlNode dateDebutNode = responseNode.getChildElement('dateDebut', null);

            this.dateDebut = (dateDebutNode == null) ? null : Date.valueOf(dateDebutNode.getText());

            nodesParsed.add(dateDebutNode);

            //System.debug(this.dateDebut);

            DOM.XmlNode dateFinNode = responseNode.getChildElement('dateFin', null);

            this.dateFin = (dateFinNode == null) ? null : Date.valueOf(dateFinNode.getText());

            nodesParsed.add(dateFinNode);

            //System.debug(this.dateFin);

            DOM.XmlNode uniteConsommationNode = responseNode.getChildElement('uniteConsommation', null);

            this.uniteConsommation = (uniteConsommationNode == null) ? null : uniteConsommationNode.getText();

            nodesParsed.add(uniteConsommationNode);

            //System.debug(this.uniteConsommation);

            DOM.XmlNode remiseAnnuelleHTNode = responseNode.getChildElement('remiseAnnuelleHT', null);

            this.remiseAnnuelleHT = (remiseAnnuelleHTNode == null) ? null : remiseAnnuelleHTNode.getText();

            nodesParsed.add(remiseAnnuelleHTNode);

            //System.debug(this.remiseAnnuelleHT);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){



            gdfsuezComBefChpPivotContrat.listeValeursDt typeTSEObj = this.typeTSE;

            Dom.XmlNode typeTSENode = outerNode.addChildElement('typeTSE', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(typeTSEObj != null){

                typeTSEObj.populateXmlNode(typeTSENode);

            }

            Dom.XmlNode TSEActifNode = outerNode.addChildElement('TSEActif', 'http://gdfsuez.com/bef/calypso/contrat', '');

            TSEActifNode.addTextNode((this.TSEActif == null) ? '' : String.valueOf(this.TSEActif));

            Dom.XmlNode dateDebutNode = outerNode.addChildElement('dateDebut', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateDebutNode.addTextNode((this.dateDebut == null) ? '' : String.valueOf(this.dateDebut));

            Dom.XmlNode dateFinNode = outerNode.addChildElement('dateFin', 'http://gdfsuez.com/bef/calypso/contrat', '');

            dateFinNode.addTextNode((this.dateFin == null) ? '' : String.valueOf(this.dateFin));

            Dom.XmlNode uniteConsommationNode = outerNode.addChildElement('uniteConsommation', 'http://gdfsuez.com/bef/calypso/contrat', '');

            uniteConsommationNode.addTextNode((this.uniteConsommation == null) ? '' : this.uniteConsommation);

            Dom.XmlNode remiseAnnuelleHTNode = outerNode.addChildElement('remiseAnnuelleHT', 'http://gdfsuez.com/bef/calypso/contrat', '');

            remiseAnnuelleHTNode.addTextNode((this.remiseAnnuelleHT == null) ? '' : this.remiseAnnuelleHT);

        }

    }

    public class CompteClientAbstract {

        public String idCompteClient;

        private String[] idCompteClient_type_info = new String[]{'idCompteClient','http://gdfsuez.com/bef/calypso/compteclient','','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/compteclient','true','false'};

        private String[] field_order_type_info = new String[]{'idCompteClient'};

        public CompteClientAbstract(){

        }

        public CompteClientAbstract(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode idCompteClientNode = responseNode.getChildElement('idCompteClient', null);

            this.idCompteClient = (idCompteClientNode == null) ? null : idCompteClientNode.getText();

            nodesParsed.add(idCompteClientNode);

            //System.debug(this.idCompteClient);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode idCompteClientNode = outerNode.addChildElement('idCompteClient', 'http://gdfsuez.com/bef/calypso/compteclient', '');

            idCompteClientNode.addTextNode((this.idCompteClient == null) ? '' : this.idCompteClient);

        }

    }

    public class CompteClientRoot {

        public String idCompteClient;

        private String[] idCompteClient_type_info = new String[]{'idCompteClient','http://gdfsuez.com/bef/calypso/compteclient','','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/compteclient','true','false'};

        private String[] field_order_type_info = new String[]{'idCompteClient'};

        public CompteClientRoot(){

        }

        public CompteClientRoot(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode idCompteClientNode = responseNode.getChildElement('idCompteClient', null);

            this.idCompteClient = (idCompteClientNode == null) ? null : idCompteClientNode.getText();

            nodesParsed.add(idCompteClientNode);

            //System.debug(this.idCompteClient);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode idCompteClientNode = outerNode.addChildElement('idCompteClient', 'http://gdfsuez.com/bef/calypso/compteclient', '');

            idCompteClientNode.addTextNode((this.idCompteClient == null) ? '' : this.idCompteClient);

        }

    }

    public class PackAbstract {

        public String idPack;

        private String[] idPack_type_info = new String[]{'idPack','http://gdfsuez.com/bef/calypso/pack','','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/pack','true','false'};

        private String[] field_order_type_info = new String[]{'idPack'};

        public PackAbstract(){

        }

        public PackAbstract(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode idPackNode = responseNode.getChildElement('idPack', null);

            this.idPack = (idPackNode == null) ? null : idPackNode.getText();

            nodesParsed.add(idPackNode);

            //System.debug(this.idPack);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode idPackNode = outerNode.addChildElement('idPack', 'http://gdfsuez.com/bef/calypso/pack', '');

            idPackNode.addTextNode((this.idPack == null) ? '' : this.idPack);

        }

    }



    public class PersonneAbstract {

        public String idPersonne;

        private String[] idPersonne_type_info = new String[]{'idPersonne','http://gdfsuez.com/bef/calypso/personne','','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/personne','true','false'};

        private String[] field_order_type_info = new String[]{'idPersonne'};

        public PersonneAbstract(){

        }

        public PersonneAbstract(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode idPersonneNode = responseNode.getChildElement('idPersonne', null);

            this.idPersonne = (idPersonneNode == null) ? null : idPersonneNode.getText();

            nodesParsed.add(idPersonneNode);

            //System.debug(this.idPersonne);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode idPersonneNode = outerNode.addChildElement('idPersonne', 'http://gdfsuez.com/bef/calypso/personne', '');

            idPersonneNode.addTextNode((this.idPersonne == null) ? '' : this.idPersonne);

        }

    }



    public class PDLAbstract {

        public String idPDL;

        private String[] idPDL_type_info = new String[]{'idPDL','http://gdfsuez.com/bef/calypso/pdl','','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/pdl','true','false'};

        private String[] field_order_type_info = new String[]{'idPDL'};

        public PDLAbstract(){

        }

        public PDLAbstract(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode idPDLNode = responseNode.getChildElement('idPDL', null);

            this.idPDL = (idPDLNode == null) ? null : idPDLNode.getText();

            nodesParsed.add(idPDLNode);

           //System.debug(this.idPDL);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode idPDLNode = outerNode.addChildElement('idPDL', 'http://gdfsuez.com/bef/calypso/pdl', '');

            idPDLNode.addTextNode((this.idPDL == null) ? '' : this.idPDL);

        }

    }



    public class OffreAbstract {

        public String idOffre;

        private String[] idOffre_type_info = new String[]{'idOffre','http://gdfsuez.com/bef/calypso/offre','','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/offre','true','false'};

        private String[] field_order_type_info = new String[]{'idOffre'};

        public OffreAbstract(){

        }

        public OffreAbstract(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode idOffreNode = responseNode.getChildElement('idOffre', null);

            this.idOffre = (idOffreNode == null) ? null : idOffreNode.getText();

            nodesParsed.add(idOffreNode);

            //System.debug(this.idOffre);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode idOffreNode = outerNode.addChildElement('idOffre', 'http://gdfsuez.com/bef/calypso/offre', '');

            idOffreNode.addTextNode((this.idOffre == null) ? '' : this.idOffre);

        }

    }

    public class dateSupplementaire {

        public gdfsuezComBefChpPivotContrat.listeValeursDt motifDate;

        public DateTime valeurDate;

        private String[] motifDate_type_info = new String[]{'motifDate','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] valeurDate_type_info = new String[]{'valeurDate','http://gdfsuez.com/bef/calypso/contrat','dateTime','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'motifDate','valeurDate'};

        public dateSupplementaire(){

        }

        public dateSupplementaire(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode motifDateNode = responseNode.getChildElement('motifDate', null);

            if(motifDateNode == null){

                this.motifDate = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt motifDateObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(motifDateNode);

                nodesParsed.add(motifDateNode);

                this.motifDate = motifDateObj;

            }

            //System.debug(this.motifDate);

            DOM.XmlNode valeurDateNode = responseNode.getChildElement('valeurDate', null);

            this.valeurDate = (valeurDateNode == null) ? null :  gdfsuezComBefCalypsoContrat.parseDateTime(valeurDateNode.getText());

            nodesParsed.add(valeurDateNode);

            //System.debug(this.valeurDate);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){



            gdfsuezComBefChpPivotContrat.listeValeursDt motifDateObj = this.motifDate;

            Dom.XmlNode motifDateNode = outerNode.addChildElement('motifDate', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(motifDateObj != null){

                motifDateObj.populateXmlNode(motifDateNode);

            }

            Dom.XmlNode valeurDateNode = outerNode.addChildElement('valeurDate', 'http://gdfsuez.com/bef/calypso/contrat', '');

            valeurDateNode.addTextNode((this.valeurDate == null) ? '' : String.valueOf(this.valeurDate));

        }

    }

    public class listeCompteClientDt {

        public gdfsuezComBefCalypsoContrat.CompteClientRoot[] compteClient;

        private String[] compteClient_type_info = new String[]{'compteClient','http://gdfsuez.com/bef/calypso/contrat','CompteClientRoot','1','-1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'compteClient'};

        public listeCompteClientDt(){

        }

        public listeCompteClientDt(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            Dom.XmlNode[] compteClientChildren = responseNode.getChildElements();

            gdfsuezComBefCalypsoContrat.CompteClientRoot[] compteClientList = new List<gdfsuezComBefCalypsoContrat.CompteClientRoot>();

            if(compteClientChildren == null || compteClientChildren.size() == 0){

                this.compteClient = null;

            } else{

                for(DOM.XmlNode childNode : compteClientChildren){

                    if(childNode.GetName() == 'compteClient'){

                        gdfsuezComBefCalypsoContrat.CompteClientRoot Obj = new gdfsuezComBefCalypsoContrat.CompteClientRoot(childNode);

                        compteClientList.Add(Obj);

                        nodesParsed.add(childNode);

                    }

                }

                this.compteClient = compteClientList;

            }

            //System.debug(this.compteClient);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){



            gdfsuezComBefCalypsoContrat.CompteClientRoot[] compteClientObj = this.compteClient;

            if(compteClientObj != null){

                for(gdfsuezComBefCalypsoContrat.CompteClientRoot element: compteClientObj){

                    Dom.XmlNode compteClientNode = outerNode.addChildElement('compteClient', 'http://gdfsuez.com/bef/calypso/contrat', '');

                    element.populateXmlNode(compteClientNode);

                }

            }

        }

    }

    public class RemiseOneShotDt {

        public Double montantHT;

        public gdfsuezComBefChpPivotContrat.listeValeursDt typeRemiseOneShot;

        private String[] montantHT_type_info = new String[]{'montantHT','http://gdfsuez.com/bef/calypso/contrat','float','0','1','false'};

        private String[] typeRemiseOneShot_type_info = new String[]{'typeRemiseOneShot','http://gdfsuez.com/bef/calypso/contrat','listeValeursDt','0','1','false'};

        private String[] apex_schema_type_info = new String[]{'http://gdfsuez.com/bef/calypso/contrat','true','false'};

        private String[] field_order_type_info = new String[]{'montantHT','typeRemiseOneShot'};

        public RemiseOneShotDt(){

        }

        public RemiseOneShotDt(DOM.XmlNode responseNode){

            Set<DOM.XmlNode> nodesParsed = new Set<DOM.XmlNode>();

            DOM.XmlNode montantHTNode = responseNode.getChildElement('montantHT', null);

            this.montantHT = (montantHTNode == null) ? null : Double.valueOf(montantHTNode.getText());

            nodesParsed.add(montantHTNode);

            //System.debug(this.montantHT);

            DOM.XmlNode typeRemiseOneShotNode = responseNode.getChildElement('typeRemiseOneShot', null);

            if(typeRemiseOneShotNode == null){

                this.typeRemiseOneShot = null;

            } else{

                gdfsuezComBefChpPivotContrat.listeValeursDt typeRemiseOneShotObj = new gdfsuezComBefChpPivotContrat.listeValeursDt(typeRemiseOneShotNode);

               nodesParsed.add(typeRemiseOneShotNode);

                this.typeRemiseOneShot = typeRemiseOneShotObj;

            }

            //System.debug(this.typeRemiseOneShot);

        }

        public void populateXmlNode(Dom.XmlNode outerNode){

            Dom.XmlNode montantHTNode = outerNode.addChildElement('montantHT', 'http://gdfsuez.com/bef/calypso/contrat', '');

            montantHTNode.addTextNode((this.montantHT == null) ? '' : String.valueOf(this.montantHT));



            gdfsuezComBefChpPivotContrat.listeValeursDt typeRemiseOneShotObj = this.typeRemiseOneShot;

            Dom.XmlNode typeRemiseOneShotNode = outerNode.addChildElement('typeRemiseOneShot', 'http://gdfsuez.com/bef/calypso/contrat', '');

            if(typeRemiseOneShotObj != null){

                typeRemiseOneShotObj.populateXmlNode(typeRemiseOneShotNode);

            }

        }

    }
}