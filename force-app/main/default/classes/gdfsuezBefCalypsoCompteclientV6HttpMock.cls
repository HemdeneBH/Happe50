//Generated by FuseIT WSDL2Apex (http://www.fuseit.com/Solutions/SFDC-Explorer/Help-WSDL-Parser.aspx)

@isTest
global class gdfsuezBefCalypsoCompteclientV6HttpMock implements HttpCalloutMock {
	global HTTPResponse respond(HTTPRequest req){
		HttpResponse res = new HttpResponse();
		res.setHeader('Content-Type', 'application/json');
		DOM.Document responseDoc = new DOM.Document();
		DOM.XmlNode envelope = responseDoc.createRootElement('Envelope', 'http://schemas.xmlsoap.org/soap/envelope/', 's');
		DOM.XmlNode bodyNode = envelope.addChildElement('Body', 'http://schemas.xmlsoap.org/soap/envelope/', null);
        
		DOM.Document reqDoc = req.getBodyDocument();
		DOM.XmlNode rootNode = reqDoc.getRootElement();
		DOM.XmlNode reqBodyNode = rootNode.getChildElement('Body','http://schemas.xmlsoap.org/soap/envelope/');
		DOM.XmlNode[] methodNode = reqBodyNode.getChildElements();
		if(methodNode[0].getName() == 'ActiverServiceGestion'){
			DOM.XmlNode ResponseNode = bodyNode.addChildElement('activerServiceGestionResponse', 'http://gdfsuez/bef/calypso/compteclient/v6', null);
		}
		else if(methodNode[0].getName() == 'CreerCompteClient'){
			DOM.XmlNode ResponseNode = bodyNode.addChildElement('creerCompteClientResponse', 'http://gdfsuez/bef/calypso/compteclient/v6', null);
		}
		else if(methodNode[0].getName() == 'DesactiverServiceGestion'){
			DOM.XmlNode ResponseNode = bodyNode.addChildElement('desactiverServiceGestionResponse', 'http://gdfsuez/bef/calypso/compteclient/v6', null);
		}
		else if(methodNode[0].getName() == 'LireCompteClient'){
			DOM.XmlNode ResponseNode = bodyNode.addChildElement('lireCompteClientResponse', 'http://gdfsuez/bef/calypso/compteclient/v6', null);
		}
		else if(methodNode[0].getName() == 'LireTypologieFR'){
			DOM.XmlNode ResponseNode = bodyNode.addChildElement('lireTypologieFRResponse', 'http://gdfsuez/bef/calypso/compteclient/v6', null);
		}
		else if(methodNode[0].getName() == 'MajCompteClient'){
			DOM.XmlNode ResponseNode = bodyNode.addChildElement('majCompteClientResponse', 'http://gdfsuez/bef/calypso/compteclient/v6', null);
		}
		else if(methodNode[0].getName() == 'RechercherCompteClient'){
			DOM.XmlNode ResponseNode = bodyNode.addChildElement('rechercherCompteClientResponse', 'http://gdfsuez/bef/calypso/compteclient/v6', null);
		}
		res.setBody(responseDoc.toXmlString());
		res.setStatusCode(200);
		return res;
	}
}