//Generated by FuseIT WSDL2Apex (http://www.fuseit.com/Solutions/SFDC-Explorer/Help-WSDL-Parser.aspx)

@isTest
public class gdfsuezComBefChpCalypsoSessionV1MockImpl implements WebServiceMock {
	public void doInvoke(
			Object stub,
			Object request,
			Map<String, Object> response,
			String endpoint,
			String soapAction,
			String requestName,
			String responseNS,
			String responseName,
			String responseType) {
	   
		System.debug(LoggingLevel.INFO, 'gdfsuezComBefChpCalypsoSessionV1MockImpl.doInvoke() - ' +
			'\n request: ' + request +
			'\n response: ' + response +
			'\n endpoint: ' + endpoint +
			'\n soapAction: ' + soapAction +
			'\n requestName: ' + requestName +
			'\n responseNS: ' + responseNS +
			'\n responseName: ' + responseName +
			'\n responseType: ' + responseType);

		if(request instanceOf gdfsuezComBefChpCalypsoSessionV1New.killSession_element) {
			response.put( 'response_x', new gdfsuezComBefChpCalypsoSessionV1New.killSessionResponse_element());
		}
		else if(request instanceOf gdfsuezComBefChpCalypsoSessionV1New.openSession_element) {
			response.put( 'response_x', new gdfsuezComBefChpCalypsoSessionV1New.openSessionResponse_element());
		}
		else if(request instanceOf gdfsuezComBefChpCalypsoSessionV1New.verifySession_element) {
			response.put( 'response_x', new gdfsuezComBefChpCalypsoSessionV1New.verifySessionResponse_element());
		}
	}
}