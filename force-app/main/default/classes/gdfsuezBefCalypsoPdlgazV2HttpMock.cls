//Generated by FuseIT WSDL2Apex (http://www.fuseit.com/Solutions/SFDC-Explorer/Help-WSDL-Parser.aspx)

@isTest
global class gdfsuezBefCalypsoPdlgazV2HttpMock implements HttpCalloutMock {
	global HTTPResponse respond(HTTPRequest req){
		HttpResponse res = new HttpResponse();
		res.setHeader('Content-Type', 'application/json');
		DOM.Document responseDoc = new DOM.Document();
		DOM.XmlNode envelope = responseDoc.createRootElement('Envelope', 'http://schemas.xmlsoap.org/soap/envelope/', 's');
		DOM.XmlNode bodyNode = envelope.addChildElement('Body', 'http://schemas.xmlsoap.org/soap/envelope/', null);
        
		DOM.Document reqDoc = req.getBodyDocument();
		DOM.XmlNode rootNode = reqDoc.getRootElement();
		DOM.XmlNode reqBodyNode = rootNode.getChildElement('Body','http://schemas.xmlsoap.org/soap/envelope/');
		DOM.XmlNode[] methodNode = reqBodyNode.getChildElements();
		if(methodNode[0].getName() == 'CreerPDLGaz'){
			DOM.XmlNode ResponseNode = bodyNode.addChildElement('creerPDLGazResponse', 'http://gdfsuez/bef/calypso/pdlgaz/v2', null);
		}
		else if(methodNode[0].getName() == 'LirePDLGaz'){
			DOM.XmlNode ResponseNode = bodyNode.addChildElement('lirePDLGazResponse', 'http://gdfsuez/bef/calypso/pdlgaz/v2', null);
            DOM.XmlNode output =    ResponseNode.addChildElement('lirePDLGazOutput', null, null) ;
            DOM.XmlNode donnesPDl =    output.addChildElement('donneesPDL', null, null) ;
            
           gdfsuezBefCalypsoPdlgazV2.PDLGaz_dernieresReleves_element dernieresRelevesObj = new gdfsuezBefCalypsoPdlgazV2.PDLGaz_dernieresReleves_element();
            
			Dom.XmlNode dernieresRelevesNode = donnesPDl.addChildElement('dernieresReleves', null, null);
            List<gdfsuezBefCalypsoPdlgazV2.releve6M_1MDt> elements = new   List<gdfsuezBefCalypsoPdlgazV2.releve6M_1MDt>() ;
           
    
     
            
            gdfsuezBefCalypsoPdlgazV2.releve6M_1MDt element = new gdfsuezBefCalypsoPdlgazV2.releve6M_1MDt() ;
            element.dateFin = system.now() ;
            gdfsuezBefCalypsoPdlgazV2.releve6M_1MDt element1 = new   gdfsuezBefCalypsoPdlgazV2.releve6M_1MDt() ;
            element1.dateFin = system.now() +10;
            elements.add(element);
            elements.add(element1) ;
            dernieresRelevesObj.releve6M_1M = elements ;
			if(dernieresRelevesObj != null){
				dernieresRelevesObj.populateXmlNode(dernieresRelevesNode);
			}

           gdfsuezBefCalypsoPdlgazV2.PDLGaz_donneesContractuelles_element donneesContractuellesObj = new gdfsuezBefCalypsoPdlgazV2.PDLGaz_donneesContractuelles_element() ;
			Dom.XmlNode donneesContractuellesNode = donnesPDl.addChildElement('donneesContractuelles', '', '');
			if(donneesContractuellesObj != null){
				donneesContractuellesObj.populateXmlNode(donneesContractuellesNode);
			}
            
            system.debug('response'+responseDoc.toXmlString());
            
		}
		else if(methodNode[0].getName() == 'MajPDLGaz'){
			DOM.XmlNode ResponseNode = bodyNode.addChildElement('majPDLGazResponse', 'http://gdfsuez/bef/calypso/pdlgaz/v2', null);
		}
		res.setBody(responseDoc.toXmlString());
		res.setStatusCode(200);
		return res;
	}
}